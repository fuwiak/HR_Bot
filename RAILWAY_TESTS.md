# 🧪 Тесты на Railway - Краткая инструкция

## Быстрый старт

### 1. Включить тесты при деплое

В Railway Dashboard → **Variables** добавьте:

```bash
RUN_TESTS=true
```

### 2. Просмотр результатов

**Railway Dashboard** → **Deployments** → **Logs**

Ищите секцию:
```
🧪 ТЕСТ: test_forsight_price
✅ ТЕСТ test_forsight_price: ПРОЙДЕН
📊 ИТОГОВЫЙ ОТЧЕТ
```

## Что тестируется

### ✅ test_forsight_price.py
- Проверка цены Форсайт-сессии: **90 000 рублей**
- LangGraph RAG извлечение цен
- Прямой поиск в Qdrant
- Проверка базы данных

### ✅ test_langgraph_rag.py
- Работа LangGraph RAG
- Запросы о ценах
- Валидация цен

## Запуск тестов вручную

```bash
# Через Railway CLI
railway run python run_tests.py

# Конкретный тест
railway run python test_forsight_price.py
```

## Переменные окружения

Убедитесь что установлены:
- `QDRANT_URL`
- `QDRANT_API_KEY`
- `OPENROUTER_API_KEY` (или `OPENAI_API_KEY`, `GROQ_API_KEY`)

## Отключение тестов

Установите в Railway Variables:
```bash
RUN_TESTS=false
```

Или просто удалите переменную (по умолчанию `false`).

## Логирование

Все результаты тестов логируются в:
- **Railway Logs** (stdout/stderr)
- `/tmp/test_results.json` (внутри контейнера)

## Troubleshooting

**Тесты не запускаются?**
- Проверьте `RUN_TESTS=true`
- Проверьте что `run_tests.py` скопирован

**Тесты падают?**
- Проверьте переменные окружения
- Проверьте подключение к Qdrant
- Смотрите логи на ошибки

Подробнее: `railway_test_config.md`
