"""
Proposal Generator Module
Генерация коммерческих предложений, классификация запросов и гипотез
"""
import logging
from typing import Dict, List, Optional
from datetime import datetime

log = logging.getLogger()

# Импорты для LLM и RAG
try:
    from services.helpers.llm_helper import generate_with_fallback
    from services.rag.qdrant_helper import search_service
    LLM_AVAILABLE = True
except ImportError:
    LLM_AVAILABLE = False
    log.warning("⚠️ LLM или RAG модули недоступны")

# ===================== PROMPTS =====================

CLASSIFICATION_PROMPT = """
Ты AI-ассистент для консалтинговой практики. Классифицируй запрос клиента по категориям.

Категории:
- "автоматизация" - автоматизация HR-процессов, внедрение систем
- "бизнес-анализ" - анализ бизнес-процессов, консалтинг
- "другое" - не подходит под основные категории

Ответь ТОЛЬКО в формате JSON:
{
    "category": "автоматизация" | "бизнес-анализ" | "другое",
    "confidence": 0.0-1.0,
    "keywords": ["ключевое слово 1", "ключевое слово 2"]
}

Запрос: {{request}}
"""

PROPOSAL_GENERATION_PROMPT = """
Ты — опытный HR‑консультант и руководитель HR‑проектов.
Твоя задача — писать коммерческие предложения (КП) в ответ на описание запроса заказчика по HR/оценке/развитию, в деловом, но живом и понятном стиле.

ЗОЛОТОЙ СТАНДАРТ ФОРМАТА КП - СТРОГО СЛЕДУЙ ЭТОМУ ФОРМАТУ:

1. НАЧАЛО (обязательно):
"Добрый день! Благодарю за ваш запрос. Я, Анастасия Новосёлова, с удовольствием помогу вам с [краткое описание задачи]. Учитывая специфику вашего бизнеса [если есть информация о бизнесе клиента], предлагаю следующий подход:"

2. СТРУКТУРА (всегда в таком порядке):

Кратко о задаче:  
[1-2 предложения, кратко перескажи запрос клиента и суть задачи]

Мой подход:  
[1-2 предложения о твоём подходе к решению задачи, акцент на результат]

Этапы работы:  
[Нумерованный список из 3-7 этапов, каждый этап - одно предложение с точкой в конце]
1. [Этап 1].  
2. [Этап 2].  
3. [Этап 3].  
...

Сроки:  
[Конкретные сроки в неделях, например: "Проект займёт ориентировочно 6–8 недель, в зависимости от глубины анализа и объёма изменений."]

Ориентировочный бюджет:  
[Конкретная сумма "от X рублей" или диапазон, обязательно указать что точная сумма определяется после брифинга]

Почему я:  
[2-3 предложения о твоём опыте, релевантных кейсах, адаптации под специфику клиента]
КРИТИЧЕСКИ ВАЖНО - НАЗВАНИЯ КОМПАНИЙ: 
- Если в базе знаний есть конкретные названия компаний, с которыми ты работала - ОБЯЗАТЕЛЬНО упомяни их в этом разделе
- Особое внимание к компаниям из той же отрасли, что и клиент (например, если клиент из инжиниринга - упомяни инжиниринговые компании: Rotvek, Айкон-девелопмент, ППР Эксперт, Брикмастер, Аквасторож, НОЦ МГТУ им. Баумана)
- Если клиент из логистики - упомяни RHENUS LOGISTICS, ФК-инвент
- Если клиент из IT - упомяни ФБ Консалт, Московская Школа Программистов
- Если клиент из образования - упомяни МИРЭА, НОЦ МГТУ им. Баумана, GAZ-кампус
- Если клиент из производства/металлургии - упомяни Норникель, Ростовский трубный завод, КМЗ – Урал
- Формат: "Я работала с компаниями [конкретные названия из базы знаний], что даёт мне опыт работы в вашей отрасли."
- НЕ выдумывай названия компаний - используй ТОЛЬКО те, что указаны в базе знаний
- Если в базе знаний есть несколько компаний из нужной отрасли - упомяни 2-3 наиболее релевантные

3. ЗАВЕРШЕНИЕ (обязательно):
"Предлагаю обсудить детали проекта на созвоне или в переписке. Готова ответить на ваши вопросы и предложить оптимальное решение."

КРИТИЧЕСКИ ВАЖНО - О КОНСУЛЬТАНТЕ:
- Работает ОДИН консультант - Анастасия Новосёлова
- Используй единственное число: "я", "мне", "я проведу", "я подготовлю", "я выполню"
- КРИТИЧЕСКИ ВАЖНО: ВСЕГДА используй ЖЕНСКУЮ форму глаголов (работала, сделала, помогла, подготовила, готова, рада и т.д.)
- Всегда представляйся: "Я, Анастасия Новосёлова, с удовольствием помогу вам..."
- НИКОГДА не используй "мы", "наша команда", "наши специалисты" - только "я"

КРИТИЧЕСКИ ВАЖНО - ОБРАЩЕНИЕ К КЛИЕНТУ:
- ВСЕГДА используй "вы" при обращении к клиенту (вас, вам, ваш, ваша и т.д.)
- НИКОГДА не используй "ты" или "тебе" - только "вы" и соответствующие формы
- Приветствие ТОЛЬКО: "Добрый день!" или "Здравствуйте!"
- НИКОГДА не используй "Привет"

ОБЩИЕ ПРАВИЛА СТИЛЯ:
- Пиши от первого лица единственного числа («я предлагаю», «я сделаю»)
- Формулировки деловые, без воды, с акцентом на результат для бизнеса
- НИКОГДА не используй Markdown форматирование (звездочки *, подчеркивания __, заголовки #, ### и т.д.) - пиши обычным текстом
- Используй пустые строки между блоками для структуры
- Подзаголовки пиши обычным текстом с двоеточием: "Кратко о задаче:", "Мой подход:", "Этапы работы:", "Сроки:", "Ориентировочный бюджет:", "Почему я:"
- В нумерованном списке этапов используй формат: "1. [текст].  " (точка в конце, два пробела после)

ПРАВИЛА ДЛЯ РАЗНЫХ ТИПОВ ЗАПРОСОВ:

КРИТИЧЕСКИ ВАЖНО - ЦЕНЫ И БЮДЖЕТ:
- ВСЕГДА используй ТОЧНЫЕ цены из раздела "Релевантная информация из базы знаний" выше
- Если в базе знаний есть информация о ценах для данного типа услуги - используй ТОЧНО эти цены
- НЕ выдумывай цены - используй только те, что указаны в базе знаний
- Если в базе знаний нет информации о ценах, используй примерные ориентиры (только как fallback):
  * Организационная диагностика и проектирование: от 150 000 рублей
  * Прокачка отдела рекрутинга / выращивание HRD: от 200 000 рублей
  * Оценка руководителей / тендер: от 300 000 рублей
- Всегда указывай, что точная стоимость определяется после брифинга

Для организационной диагностики и проектирования:
- Этапы: диагностика → проектирование структуры → описание процессов → разработка регламентов → внедрение
- Сроки: 6-8 недель
- Бюджет: используй цену из базы знаний, если есть, иначе "от 150 000 рублей (точная стоимость определяется после брифинга)"

Для прокачки отдела рекрутинга / выращивания HRD:
- Этапы: диагностика → дизайн HR‑функции → внедрение процессов → обучение/наставничество → кураторство
- Сроки: первые полгода плотная работа, далее кураторство/сессии
- Бюджет: используй цену из базы знаний, если есть, иначе "от 200 000 рублей (точная стоимость определяется после брифинга)"

Для оценки руководителей / тендер:
- Этапы: разработка модели компетенций → формирование тестов → дизайн ассессмент‑центра → разработка 360 → проведение оценки → индивидуальные отчёты → сводный отчёт и обратная связь
- Сроки: 8-12 недель
- Бюджет: используй цену из базы знаний, если есть, иначе "от 300 000 рублей (точная стоимость определяется после брифинга)"

ОСОБОЕ:
- КРИТИЧЕСКИ ВАЖНО: Используй ВСЮ доступную информацию из запроса клиента, даже если она неполная
- НЕ проси уточнить детали - работай с тем, что есть в запросе
- Если заказчик указывает дедлайн (например, «до 19 февраля»), обязательно отрази это и предложи укрупнённый план, укладывающийся в срок
- Не уходи в теорию HR, фокусируйся на конкретных шагах, метриках и результатах
- Избегай шаблонного «бла‑бла про ценности», лучше привязывайся к задачам клиента: рост, эффективность, кадровый резерв, вовлечённость руководителей
- Если есть информация о специфике бизнеса клиента (отрасль, размер компании) - обязательно упомяни это в начале и учти в подходе
- Если в запросе указан размер компании (например, "80 чел.") - используй это в КП
- Если указана отрасль (например, "инжиниринговый центр") - обязательно упомяни это и используй релевантный опыт
- Каждый ответ формируй так, чтобы его можно было сразу отправить заказчику как черновик КП без дополнительной правки

КРИТИЧЕСКИ ВАЖНО - СРОКИ И ДАТЫ:
- ВСЕГДА используй реальные даты из информации о календаре ниже
- НЕ выдумывай даты - используй только те, что указаны в разделе "Информация о календаре"
- Если указана "ближайшая доступная дата" - используй её как минимальную дату начала
- Если указана "рекомендуемая дата начала" - используй её как оптимальную дату старта проекта
- Учитывай занятые периоды при планировании этапов
- Если клиент указывает свой дедлайн - проверь, реально ли уложиться с учетом занятых периодов

ВАЖНО: 
- Используй ТОЛЬКО информацию из базы знаний. Не выдумывай кейсы или методики.
- Для цен и бюджета ВСЕГДА приоритетно используй информацию из базы знаний - там есть актуальные прайс-листы и цены на услуги.
- Если в базе знаний есть конкретные цены для услуги - используй их ТОЧНО, не округляй и не изменяй.

КРИТИЧЕСКИ ВАЖНО - НАЗВАНИЯ КОМПАНИЙ ИЗ ОПЫТА РАБОТЫ:
- В базе знаний есть документ "Матрица..." с конкретными названиями компаний, с которыми работала Анастасия Новосёлова
- ОБЯЗАТЕЛЬНО включай конкретные названия компаний в раздел "Почему я"
- Выбирай компании из той же отрасли, что и клиент:
  * Инжиниринг/строительство: Rotvek, Айкон-девелопмент, ППР Эксперт, Брикмастер, Аквасторож, НОЦ МГТУ им. Баумана
  * Логистика/ВЭД: RHENUS LOGISTICS, ФК-инвент, Флот «Русич»
  * IT/CRM: ФБ Консалт, Московская Школа Программистов, Uminers
  * Образование: МИРЭА, НОЦ МГТУ им. Баумана, GAZ-кампус, Академия востребованных экспертов
  * Производство/металлургия: Норникель, Ростовский трубный завод, КМЗ – Урал
  * Медицина/фармацевтика: СИТИЛАБ, Семейный доктор, Русфарма
  * Финансы/банки: АКБ «Инвестторгбанк», Локо-банк
  * FMCG: Объединенные кондитеры (Рот-Фронт, Бабаевский, Красный Октябрь)
  * Нефтегаз: Газпромнефтьразвитие, Новатэк
- Формат: "Я работала с компаниями [2-3 конкретных названия из базы знаний, релевантных отрасли клиента], что даёт мне опыт работы в вашей отрасли."
- НЕ выдумывай названия компаний - используй ТОЛЬКО те, что указаны в базе знаний
- Если в базе знаний нет компаний из нужной отрасли - упомяни компании из смежных отраслей или общий опыт

{{conversation_history}}

Запрос клиента: {{request}}

Релевантная информация из базы знаний:
{{rag_context}}

Информация о календаре (реальные доступные даты):
{{calendar_info}}
"""

HYPOTHESIS_GENERATION_PROMPT = """
Ты AI-ассистент консультанта. На основе запроса клиента и похожих кейсов из базы знаний сформируй консалтинговые гипотезы.

Формат:
1. Основная гипотеза решения
2. Аналогичные кейсы из практики (если есть)
3. Рекомендуемые методики (если есть)
4. Риски и ограничения

Запрос клиента: {{request}}

Похожие кейсы из базы знаний:
{{rag_context}}
"""

# ===================== VALIDATION =====================

async def validate_lead(lead_request: str) -> Dict:
    """
    Упрощенная валидация - все пользователи бота автоматически считаются валидными
    
    Args:
        lead_request: Текст запроса от лида
    
    Returns:
        Словарь с результатами валидации (всегда "теплый" лид)
    """
    # Упрощенная логика: все пользователи бота автоматически считаются "теплыми" лидами
    result = {
        "score": 1.0,
        "status": "warm",
        "reason": "Все пользователи бота автоматически считаются лидами",
        "criteria": {
            "clarity": 1.0,
            "budget": 0.5,
            "relevance": 1.0
        }
    }
    
    log.info(f"✅ Лид валидирован: score={result.get('score')}, status={result.get('status')}")
    return result

async def classify_request(request: str) -> Dict:
    """
    Классификация запроса по категориям экспертизы
    
    Args:
        request: Текст запроса
    
    Returns:
        Словарь с категорией и уверенностью
    """
    if not LLM_AVAILABLE:
        return {
            "category": "другое",
            "confidence": 0.0,
            "keywords": []
        }
    
    try:
        prompt = CLASSIFICATION_PROMPT.replace("{{request}}", request)
        
        messages = [{"role": "user", "content": prompt}]
        response = await generate_with_fallback(messages, use_system_message=True, system_content="Ты помощник для классификации запросов. Отвечай только в формате JSON.")
        
        # Парсинг JSON ответа
        import json
        if "{" in response and "}" in response:
            json_start = response.find("{")
            json_end = response.rfind("}") + 1
            json_str = response[json_start:json_end]
            result = json.loads(json_str)
        else:
            result = {
                "category": "другое",
                "confidence": 0.0,
                "keywords": []
            }
        
        log.info(f"✅ Запрос классифицирован: {result.get('category')} (confidence: {result.get('confidence')})")
        return result
    except Exception as e:
        log.error(f"❌ Ошибка классификации запроса: {e}")
        return {
            "category": "другое",
            "confidence": 0.0,
            "keywords": []
        }

# ===================== LEAD INFORMATION EXTRACTION =====================

LEAD_EXTRACTION_PROMPT = """
Ты AI-ассистент для извлечения информации из запроса клиента. Твоя задача - извлечь ВСЮ доступную информацию из запроса и сформировать краткое summary для использования в коммерческом предложении.

Извлеки следующую информацию:
- Тип услуги/проекта (организационная диагностика, проектирование, оценка и т.д.)
- Отрасль клиента (инжиниринг, IT, логистика, производство и т.д.)
- Размер компании (количество сотрудников, если указано)
- Специфика бизнеса (чем занимается компания)
- Дедлайны или сроки (если указаны)
- Бюджет (если указан)
- Дополнительные детали или требования

Ответь ТОЛЬКО в формате JSON:
{
    "service_type": "тип услуги",
    "industry": "отрасль",
    "company_size": "размер компании",
    "business_spec": "специфика бизнеса",
    "deadline": "дедлайн или сроки",
    "budget": "бюджет",
    "additional_info": "дополнительная информация",
    "summary": "краткое summary запроса (2-3 предложения) для использования в КП"
}

Запрос клиента: {{request}}
"""

async def extract_lead_info(lead_request: str) -> Dict:
    """
    Извлекает максимум информации из запроса лида для использования в КП
    
    Args:
        lead_request: Текст запроса от лида
    
    Returns:
        Словарь с извлеченной информацией и summary
    """
    if not LLM_AVAILABLE:
        # Простое извлечение без LLM
        return {
            "service_type": "",
            "industry": "",
            "company_size": "",
            "business_spec": "",
            "deadline": "",
            "budget": "",
            "additional_info": "",
            "summary": lead_request[:200]
        }
    
    try:
        prompt = LEAD_EXTRACTION_PROMPT.replace("{{request}}", lead_request)
        messages = [{"role": "user", "content": prompt}]
        
        response = await generate_with_fallback(
            messages,
            use_system_message=True,
            system_content="Ты помощник для извлечения информации из запросов. Отвечай только в формате JSON.",
            max_tokens=500,
            temperature=0.3
        )
        
        # Парсинг JSON ответа
        import json
        if "{" in response and "}" in response:
            json_start = response.find("{")
            json_end = response.rfind("}") + 1
            json_str = response[json_start:json_end]
            result = json.loads(json_str)
        else:
            # Fallback - простое извлечение
            result = {
                "service_type": "",
                "industry": "",
                "company_size": "",
                "business_spec": "",
                "deadline": "",
                "budget": "",
                "additional_info": "",
                "summary": lead_request[:200]
            }
        
        log.info(f"✅ Информация из запроса извлечена: service_type={result.get('service_type')}, industry={result.get('industry')}")
        return result
    except Exception as e:
        log.error(f"❌ Ошибка извлечения информации из запроса: {e}")
        return {
            "service_type": "",
            "industry": "",
            "company_size": "",
            "business_spec": "",
            "deadline": "",
            "budget": "",
            "additional_info": "",
            "summary": lead_request[:200]
        }

# ===================== PROPOSAL GENERATION =====================

async def generate_proposal(lead_request: str, lead_contact: Dict, rag_results: Optional[List[Dict]] = None, conversation_history: Optional[str] = None, use_calendar: bool = True) -> str:
    """
    Генерация коммерческого предложения с использованием RAG и календаря
    
    Args:
        lead_request: Текст запроса от лида
        lead_contact: Контактная информация лида
        rag_results: Результаты поиска в RAG базе (опционально)
        conversation_history: История беседы с клиентом (опционально)
        use_calendar: Использовать информацию о календаре для реальных дат (по умолчанию True)
    
    Returns:
        Текст коммерческого предложения
    """
    log.info(f"🚀 [КП] Начало генерации КП. Запрос: {lead_request[:100]}...")
    
    if not LLM_AVAILABLE:
        log.error("❌ [КП] LLM недоступен")
        return "Извините, сервис временно недоступен."
    
    try:
        # Шаг 0: Извлечение информации из запроса
        log.info("🔍 [КП] Шаг 0: Извлечение информации из запроса")
        lead_info = await extract_lead_info(lead_request)
        log.info(f"🔍 [КП] Извлечена информация: service_type={lead_info.get('service_type')}, industry={lead_info.get('industry')}, company_size={lead_info.get('company_size')}")
        
        # Формируем расширенный запрос с извлеченной информацией
        enriched_request = lead_request
        if lead_info.get("summary"):
            enriched_request = f"{lead_request}\n\nИзвлеченная информация:\n{lead_info.get('summary')}"
            if lead_info.get("industry"):
                enriched_request += f"\nОтрасль: {lead_info.get('industry')}"
            if lead_info.get("company_size"):
                enriched_request += f"\nРазмер компании: {lead_info.get('company_size')}"
            if lead_info.get("business_spec"):
                enriched_request += f"\nСпецифика бизнеса: {lead_info.get('business_spec')}"
        
        # Шаг 1: Поиск в RAG базе
        log.info("📚 [КП] Шаг 1: Поиск релевантной информации в RAG базе")
        if rag_results is None:
            log.info(f"📚 [КП] Выполняю поиск по запросу: {enriched_request[:100]}...")
            rag_results = search_service(enriched_request, limit=5)
            log.info(f"📚 [КП] Найдено {len(rag_results) if rag_results else 0} релевантных документов")
        else:
            log.info(f"📚 [КП] Используются предоставленные RAG результаты ({len(rag_results)} документов)")
        
        # Шаг 2: Формирование контекста из RAG результатов
        log.info("📝 [КП] Шаг 2: Формирование контекста из RAG результатов")
        rag_context = ""
        if rag_results:
            rag_context = "Релевантная информация из базы знаний:\n\n"
            for i, result in enumerate(rag_results, 1):
                title = result.get("title", "Документ")
                text = result.get("text", result.get("content", ""))
                # Увеличиваем лимит до 1000 символов для документов с матрицей/компаниями, чтобы включить названия компаний
                # Если документ содержит "матриц" или "компани" - берем больше контекста
                if "матриц" in title.lower() or "матриц" in text.lower()[:200] or "компани" in text.lower()[:200]:
                    text_snippet = text[:1500]  # Больше контекста для документов с компаниями
                    log.info(f"📝 [КП] Документ '{title}' содержит информацию о компаниях, используем расширенный контекст (1500 символов)")
                else:
                    text_snippet = text[:800]  # Увеличено с 500 до 800 для обычных документов
                score = result.get("score", 0)
                rag_context += f"{i}. {title} (релевантность: {score:.2f})\n{text_snippet}\n\n"
                log.info(f"📝 [КП] Добавлен документ {i}: {title} (score: {score:.2f}, длина: {len(text_snippet)} символов)")
            log.info(f"📝 [КП] Контекст RAG сформирован ({len(rag_context)} символов)")
        else:
            rag_context = "В базе знаний не найдено релевантной информации для данного запроса."
            log.warning("⚠️ [КП] В базе знаний не найдено релевантной информации")
        
        # Шаг 3: Формирование контекста истории беседы
        log.info("💬 [КП] Шаг 3: Формирование контекста истории беседы")
        history_context = ""
        if conversation_history and conversation_history.strip():
            history_context = f"История беседы с клиентом:\n{conversation_history}\n\n"
            log.info(f"💬 [КП] История беседы добавлена ({len(conversation_history)} символов)")
        else:
            history_context = ""
            log.info("💬 [КП] История беседы отсутствует или пуста")
        
        # Шаг 4: Получение информации о календаре
        log.info("📅 [КП] Шаг 4: Получение информации о календаре")
        calendar_info_text = ""
        if use_calendar:
            try:
                from services.helpers.calendar_helper import format_calendar_info_for_prompt
                calendar_info_text = await format_calendar_info_for_prompt()
                log.info(f"📅 [КП] Информация о календаре получена ({len(calendar_info_text)} символов)")
            except Exception as e:
                log.warning(f"⚠️ [КП] Ошибка получения информации о календаре: {e}")
                calendar_info_text = "Информация о календаре недоступна. Используй примерные сроки в неделях."
        else:
            calendar_info_text = "Информация о календаре не запрашивалась. Используй примерные сроки в неделях."
            log.info("📅 [КП] Использование календаря отключено")
        
        # Шаг 5: Формирование промпта
        log.info("📋 [КП] Шаг 5: Формирование промпта для LLM")
        # Используем enriched_request с извлеченной информацией
        prompt = PROPOSAL_GENERATION_PROMPT.replace("{{conversation_history}}", history_context).replace("{{request}}", enriched_request).replace("{{rag_context}}", rag_context).replace("{{calendar_info}}", calendar_info_text)
        log.info(f"📋 [КП] Промпт сформирован ({len(prompt)} символов)")
        
        # Шаг 6: Генерация через LLM
        log.info("🤖 [КП] Шаг 6: Отправка запроса в LLM для генерации КП")
        messages = [{"role": "user", "content": prompt}]
        proposal = await generate_with_fallback(
            messages,
            use_system_message=True,
            system_content="Ты помощник консультанта для генерации коммерческих предложений. СТРОГО следуй золотому стандарту формата КП. Начинай с 'Добрый день! Благодарю за ваш запрос. Я, Анастасия Новосёлова, с удовольствием помогу вам...' Используй структуру: Кратко о задаче, Мой подход, Этапы работы (нумерованный список), Сроки, Ориентировочный бюджет, Почему я. Завершай: 'Предлагаю обсудить детали проекта на созвоне или в переписке. Готова ответить на ваши вопросы и предложить оптимальное решение.' КРИТИЧЕСКИ ВАЖНО: Работает ОДИН консультант - Анастасия Новосёлова. Используй единственное число (я, мне), НИКОГДА не используй множественное число (мы, наша команда). ВСЕГДА используй ЖЕНСКУЮ форму глаголов (работала, сделала, помогла, подготовила, готова, рада и т.д.). ВСЕГДА используй 'вы' при обращении к клиенту, НИКОГДА не используй 'ты'. НИКОГДА не используй Markdown форматирование - пиши обычным текстом. КРИТИЧЕСКИ ВАЖНО - ИСПОЛЬЗОВАНИЕ ИНФОРМАЦИИ: Используй ВСЮ доступную информацию из запроса клиента, даже если она неполная. НЕ проси уточнить детали - работай с тем, что есть. Если указан размер компании (например, '80 чел.') - используй это. Если указана отрасль (например, 'инжиниринговый центр') - обязательно упомяни это. ДЛЯ ЦЕН: ВСЕГДА используй точные цены из базы знаний, если они там есть. НЕ выдумывай и НЕ округляй цены. ДЛЯ КОМПАНИЙ: В разделе 'Почему я' ОБЯЗАТЕЛЬНО упомяни 2-3 конкретных названия компаний из базы знаний, релевантных отрасли клиента. Если клиент из инжиниринга - упомяни Rotvek, Айкон-девелопмент, ППР Эксперт, Брикмастер, Аквасторож. Если из логистики - RHENUS LOGISTICS, ФК-инвент. Если из IT - ФБ Консалт, Московская Школа Программистов. Если из производства - Норникель, Ростовский трубный завод, КМЗ – Урал. Формат: 'Я работала с компаниями [конкретные названия], что даёт мне опыт работы в вашей отрасли.' НЕ выдумывай названия компаний - используй ТОЛЬКО те, что указаны в базе знаний.",
            max_tokens=3000,
            temperature=0.7
        )
        
        log.info(f"✅ [КП] Шаг 7: КП успешно сгенерировано (длина: {len(proposal)} символов)")
        log.info(f"✅ [КП] Процесс генерации КП завершен успешно")
        return proposal
    except Exception as e:
        log.error(f"❌ [КП] Ошибка генерации КП на этапе: {e}")
        import traceback
        log.error(f"❌ [КП] Traceback: {traceback.format_exc()}")
        return f"Извините, произошла ошибка при генерации коммерческого предложения. Ошибка: {str(e)}"

async def generate_hypothesis(lead_request: str, rag_results: Optional[List[Dict]] = None) -> str:
    """
    Генерация консалтинговых гипотез на основе запроса и базы знаний
    
    Args:
        lead_request: Текст запроса
        rag_results: Результаты поиска в RAG базе
    
    Returns:
        Текст с гипотезами
    """
    if not LLM_AVAILABLE:
        return "Извините, сервис временно недоступен."
    
    try:
        # Если RAG результаты не предоставлены, ищем их
        if rag_results is None:
            rag_results = search_service(lead_request, limit=5)
        
        # Формируем контекст из похожих кейсов
        rag_context = ""
        if rag_results:
            rag_context = "Похожие кейсы и методики:\n\n"
            for i, result in enumerate(rag_results, 1):
                title = result.get("title", "Кейс")
                text = result.get("text", result.get("content", ""))[:500]
                category = result.get("category", "кейс")
                rag_context += f"{i}. [{category}] {title}\n{text}\n\n"
        else:
            rag_context = "В базе знаний не найдено похожих кейсов."
        
        prompt = HYPOTHESIS_GENERATION_PROMPT.replace("{{request}}", lead_request).replace("{{rag_context}}", rag_context)
        
        messages = [{"role": "user", "content": prompt}]
        hypothesis = await generate_with_fallback(
            messages,
            use_system_message=True,
            system_content="Ты помощник консультанта для генерации гипотез. Используй опыт из базы знаний.",
            max_tokens=2000,
            temperature=0.7
        )
        
        log.info(f"✅ Гипотеза сгенерирована (длина: {len(hypothesis)} символов)")
        return hypothesis
    except Exception as e:
        log.error(f"❌ Ошибка генерации гипотезы: {e}")
        return f"Извините, произошла ошибка при генерации гипотез. Ошибка: {str(e)}"


# ===================== LEAD DETECTION =====================

async def detect_lead(message: str) -> Dict:
    """
    Упрощенное определение лида - все пользователи бота считаются лидами
    
    Args:
        message: Текст сообщения пользователя
    
    Returns:
        Словарь с результатами (всегда is_lead=True):
        - is_lead: bool - всегда True
        - confidence: float - всегда 1.0
        - reason: str - причина классификации
    """
    # Упрощенная логика: все пользователи бота автоматически считаются лидами
    result = {
        "is_lead": True,
        "confidence": 1.0,
        "reason": "Все пользователи бота автоматически считаются лидами"
    }
    
    log.info(f"✅ Сообщение классифицировано как лид (confidence: {result.get('confidence'):.2f})")
    return result
























