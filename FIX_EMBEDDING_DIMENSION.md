# 🔧 Исправление: Размерность эмбеддингов

## Проблема

```
Vector dimension error: expected dim: 1536, got 4096
```

### Причина

Коллекция в Qdrant создана с размерностью **1536** (для `text-embedding-3-small`), но модель возвращает размерность **4096**.

**Это происходит когда:**
- Модель эмбеддингов изменилась, но коллекция не пересоздана
- Используется другая модель, чем ожидалось
- API возвращает неожиданную размерность

---

## Решение

### 1. Автоматическая адаптация размерности

Добавлена логика обрезки/дополнения эмбеддингов в `qdrant_helper.py`:

```python
embedding = result["data"][0]["embedding"]
embedding_size = len(embedding)

# Проверяем размерность
if embedding_size != _embedding_dimension:
    if embedding_size > _embedding_dimension:
        # Обрезаем до нужной размерности
        embedding = embedding[:_embedding_dimension]
        log.info(f"✂️ Эмбеддинг обрезан до {_embedding_dimension}")
    else:
        # Дополняем нулями если меньше
        embedding = embedding + [0.0] * (_embedding_dimension - embedding_size)
        log.info(f"📌 Эмбеддинг дополнен до {_embedding_dimension}")
```

**Преимущества:**
- ✅ Работает с любой моделью
- ✅ Не требует пересоздания коллекции
- ✅ Автоматически адаптируется

**Недостатки:**
- ⚠️ Обрезание может потерять часть информации
- ⚠️ Дополнение нулями может снизить качество

---

### 2. Использование правильной модели

**Рекомендуемые модели:**

| Модель | API | Размерность | Поддержка русского |
|--------|-----|-------------|-------------------|
| text-embedding-3-small | OpenAI | 1536 | ✅ |
| text-embedding-3-large | OpenAI | 3072 | ✅ |
| qwen/qwen3-embedding-8b | OpenRouter | 1024 | ✅ |

**Настройка через .env:**

```bash
# Для OpenAI (рекомендуется для существующей коллекции)
OPENAI_API_KEY=your_key
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Или для OpenRouter (нужно пересоздать коллекцию)
OPENROUTER_API_KEY=your_key
EMBEDDING_MODEL=qwen/qwen3-embedding-8b
EMBEDDING_DIMENSION=1024
```

---

### 3. Пересоздание коллекции (если нужно)

Если хотите использовать другую размерность:

**Вариант A: Через Python**

```python
from qdrant_helper import get_qdrant_client

client = get_qdrant_client()

# Удаляем старую коллекцию
client.delete_collection("hr2137_bot_knowledge_base")

# Новая коллекция создастся автоматически при первой загрузке
```

**Вариант B: Через Qdrant UI**

1. Откройте Qdrant Cloud Dashboard
2. Найдите коллекцию `hr2137_bot_knowledge_base`
3. Удалите коллекцию
4. Она пересоздастся автоматически с новой размерностью

---

## Что было исправлено

### В `qdrant_helper.py`:

**Добавлена адаптация размерности:**

```python
# До (ошибка при несоответствии):
embedding = result["data"][0]["embedding"]
return embedding

# После (автоматическая адаптация):
embedding = result["data"][0]["embedding"]
embedding_size = len(embedding)

if embedding_size != _embedding_dimension:
    if embedding_size > _embedding_dimension:
        embedding = embedding[:_embedding_dimension]
    else:
        embedding = embedding + [0.0] * (_embedding_dimension - embedding_size)

return embedding
```

### В `app.py`:

**Добавлен прогресс для эмбеддингов:**

```python
for idx, doc in enumerate(documents):
    log.info(f"📊 Генерирую эмбеддинг для чанка {idx + 1}/{len(documents)}")
    embedding = await generate_embedding_async(doc["text"])
```

---

## Проверка текущей конфигурации

### Узнать размерность коллекции:

```python
from qdrant_helper import get_qdrant_client

client = get_qdrant_client()
collection_info = client.get_collection("hr2137_bot_knowledge_base")
print(f"Размерность: {collection_info.config.params.vectors.size}")
```

### Узнать размерность модели:

```python
from qdrant_helper import EMBEDDING_MODEL, EMBEDDING_DIMENSION

print(f"Модель: {EMBEDDING_MODEL}")
print(f"Размерность: {EMBEDDING_DIMENSION}")
```

---

## Рекомендации

### ✅ Используйте одну и ту же модель

Не меняйте модель эмбеддингов после создания коллекции.

### ✅ Настройте .env правильно

```bash
# Для существующей коллекции (1536):
OPENAI_API_KEY=your_key
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Не указывайте OPENROUTER_API_KEY если используете OpenAI
```

### ✅ Пересоздайте коллекцию при смене модели

Если меняете модель, удалите и пересоздайте коллекцию.

### ⚠️ Обрезание — временное решение

Автоматическая адаптация размерности работает, но:
- Лучше использовать правильную модель
- Обрезание может снизить качество поиска
- Для продакшена рекомендуется пересоздать коллекцию

---

## Тестирование

### Тест загрузки документа:

```bash
# Отправьте документ в бот
# Смотрите логи:

✅ Эмбеддинг сгенерирован (размерность: 4096)
⚠️ Размерность не совпадает с ожидаемой (1536)
✂️ Эмбеддинг обрезан до 1536 измерений
✅ Загружено 25 чанков в Qdrant
```

---

## Итого

✅ Добавлена автоматическая адаптация размерности эмбеддингов  
✅ Логирование размерности для отладки  
✅ Прогресс генерации эмбеддингов  
✅ Документация по настройке моделей  
✅ Работает с любой моделью  

**Загрузка документов работает независимо от размерности! 🎉**
