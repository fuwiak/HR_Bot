# Railway конфигурация для Backend сервиса
# Этот файл используется Railway CLI для автоматической настройки

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.backend"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
