# Railway конфигурация для Frontend сервиса
# Этот файл используется Railway CLI для автоматической настройки

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
