# Сервис Frontend = AnythingLLM (не Next.js). Порт 3001.
[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# В Railway Dashboard → Settings → Networking укажи Port: 3001
