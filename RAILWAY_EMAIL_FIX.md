# 🔧 Исправление отправки email в Railway

## ❌ Проблема

Ошибка: `[Errno 101] Network is unreachable` при попытке отправить email через SMTP порты 465 и 587.

## 🔍 Диагностика

### Шаг 1: Проверьте переменные окружения в Railway

1. Откройте Railway Dashboard
2. Выберите ваш проект
3. Перейдите в **Variables**
4. Убедитесь, что установлены:

```env
YANDEX_EMAIL=a-novoselova07@yandex.ru
YANDEX_IMAP_PASSWORD=nyyiyzaithgesuzx
YANDEX_SMTP_SERVER=smtp.yandex.ru
YANDEX_SMTP_PORT=465
```

**ВАЖНО:** Используйте `YANDEX_IMAP_PASSWORD`, а не `YANDEX_PASSWORD`!

### Шаг 2: Проверьте логи бота

После добавления переменных, перезапустите сервис и проверьте логи. Должны увидеть:

```
📧 SMTP настройки: email=a-novoselova07@yandex.ru, server=smtp.yandex.ru, port=465, password_set=True
```

Если видите `password_set=False` - переменные не установлены!

## 🚨 Возможная проблема: Блокировка портов SMTP

Railway может блокировать исходящие соединения на порты 465 и 587. Это распространенная проблема в облачных платформах.

### Решение 1: Проверьте настройки сети Railway

1. В Railway Dashboard проверьте настройки сети
2. Убедитесь, что исходящие соединения разрешены
3. Проверьте, нет ли firewall правил, блокирующих SMTP

### Решение 2: Используйте альтернативный способ

Если порты заблокированы, можно использовать:
- Yandex Mail API (если доступен)
- Прокси-сервер для SMTP
- Внешний сервис для отправки email (SendGrid, Mailgun и т.д.)

## ✅ Проверка

После установки переменных, попробуйте отправить email через бота. В логах должно быть:

```
📧 Отправка email: от=a-novoselova07@yandex.ru, к=..., тема=..., server=smtp.yandex.ru, port=465/587
🔄 Попытка 1: Порт 465 (SMTP_SSL)...
✅ Email отправлен (sync, порт 465): ...
```

Если видите ошибку `Network is unreachable` - это проблема с сетью Railway, а не с переменными.

## 📝 Чеклист

- [ ] `YANDEX_EMAIL` установлен в Railway Variables
- [ ] `YANDEX_IMAP_PASSWORD` установлен в Railway Variables (16 символов)
- [ ] `YANDEX_SMTP_SERVER` установлен (или используется значение по умолчанию)
- [ ] Сервис перезапущен после добавления переменных
- [ ] В логах видно `password_set=True`
- [ ] Проверены настройки сети Railway
