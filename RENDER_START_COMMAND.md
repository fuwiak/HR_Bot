# 🚀 Start Command для Render.com

## ✅ Правильная команда запуска

Для вашего проекта используйте:

```bash
python app.py
```

---

## 📋 Полная настройка в Render.com

### Build Command:
```bash
pip install -r requirements.txt
```

### Start Command:
```bash
python app.py
```

### Environment:
- **Python 3** (Render автоматически определит версию из requirements.txt)

### Port:
- Render автоматически предоставит порт через переменную `PORT`
- Ваш код уже использует `PORT = int(os.getenv("PORT", 8080))` ✅

---

## ⚙️ Дополнительные настройки

### Если используете Webhook (рекомендуется):

Добавьте переменную окружения:
```bash
USE_WEBHOOK=true
```

Render автоматически предоставит публичный URL. Ваш код уже поддерживает это:
```python
RAILWAY_PUBLIC_DOMAIN = os.getenv("RAILWAY_PUBLIC_DOMAIN", "")
```

**Для Render.com:**
- Render предоставляет переменную `RENDER_EXTERNAL_URL` (аналог `RAILWAY_PUBLIC_DOMAIN`)
- Или добавьте переменную `WEBHOOK_URL` вручную

### Если НЕ используете Webhook:

Добавьте переменную:
```bash
USE_WEBHOOK=false
```

---

## 🔍 Проверка запуска

После деплоя проверьте логи в Render Dashboard:

**Должно быть:**
```
🚀 Запуск HR2137 Bot...
🤖 Запуск Telegram бота...
✅ Telegram Bot запущен
```

**Если ошибки:**
- Проверьте, что все переменные окружения установлены
- Проверьте, что `requirements.txt` содержит все зависимости
- Проверьте логи на наличие ошибок импорта

---

## 📝 Пример полной конфигурации Render

### Environment Variables:
```bash
# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_TOKEN=ваш_токен  # (если используется)

# Yandex Email
YANDEX_EMAIL=a-novoselova07@yandex.ru
YANDEX_IMAP_PASSWORD=ваш_пароль
YANDEX_SMTP_SERVER=smtp.yandex.ru
YANDEX_SMTP_PORT=465

# OpenRouter (если используется)
OPENROUTER_API_KEY=ваш_ключ

# Webhook (опционально)
USE_WEBHOOK=true
# Render автоматически предоставит RENDER_EXTERNAL_URL
```

### Build & Deploy:
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python app.py`
- **Environment:** `Python 3`

---

## ✅ Готово!

После настройки Render автоматически:
1. Установит зависимости из `requirements.txt`
2. Запустит `python app.py`
3. Предоставит публичный URL
4. SMTP будет работать сразу! 🎉

