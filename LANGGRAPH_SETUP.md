# Установка и настройка LangGraph RAG

## ✅ Что уже сделано

1. ✅ Добавлен `langgraph>=0.2.0` в `requirements.txt`
2. ✅ Создан файл `rag_langgraph.py` с реализацией LangGraph для точного извлечения цен
3. ✅ Интегрирован в `app.py` (Telegram бот) - автоматически используется для запросов о ценах
4. ✅ Интегрирован в `web_interface.py` (FastAPI) - эндпоинт `/rag/query`
5. ✅ Создан тест `test_langgraph_rag.py`
6. ✅ Создана документация `RAG_LANGGRAPH.md`

## 📦 Установка зависимостей

```bash
# Установить langgraph
pip install langgraph>=0.2.0

# Или установить все зависимости
pip install -r requirements.txt
```

## 🔧 Настройка Qdrant

LangGraph RAG требует подключения к Qdrant для поиска услуг.

### Вариант 1: Qdrant Cloud (рекомендуется для продакшена)

```bash
# Установите переменные окружения
export QDRANT_URL="https://your-cluster.gcp.cloud.qdrant.io"
export QDRANT_API_KEY="your-api-key"
```

### Вариант 2: Локальный Qdrant (для разработки)

```bash
# Запустите Qdrant в Docker
docker run -p 6333:6333 -p 6334:6334 qdrant/qdrant

# Или без Docker (установка Qdrant)
# См. https://qdrant.tech/documentation/quick-start/
```

## 🔑 Настройка API ключей для LLM

LangGraph RAG использует LLM для генерации ответов. Установите один из:

```bash
# OpenRouter (рекомендуется)
export OPENROUTER_API_KEY="your-key"

# Или OpenAI
export OPENAI_API_KEY="your-key"

# Или Groq
export GROQ_API_KEY="your-key"
```

## 📊 Индексация услуг в Qdrant

Перед использованием необходимо проиндексировать услуги:

```python
from qdrant_helper import index_services

# Ваш список услуг из Google Sheets или другого источника
services = [
    {
        "id": 1,
        "title": "Автоматизация и настройка HR бизнес-процессов",
        "price": 80000,
        "price_str": "от 80 000 рублей",
        "duration": 0,
        "master": "",
        "type": "hr"
    },
    {
        "id": 2,
        "title": "Стратегическая сессия",
        "price": 85000,
        "price_str": "от 85 000 рублей",
        "duration": 0,
        "master": "",
        "type": "consulting"
    },
    # ... остальные услуги
]

# Индексируем
index_services(services)
```

### Индексация из списка услуг

Используйте ваш список услуг из начала чата:

```python
services = [
    {"id": 1, "title": "Автоматизация и настройка HR бизнес-процессов", "price_str": "от 80 000 рублей"},
    {"id": 2, "title": "Стратегическая сессия", "price_str": "от 85 000 рублей"},
    {"id": 3, "title": "Оптимизация и разработка организационной структуры", "price_str": "от 80 000 рублей"},
    {"id": 4, "title": "Оптимизация / описание / реинжиниринг бизнес-процессов", "price_str": "от 90 000 рублей"},
    {"id": 5, "title": "HR-сопровождение компании / HRD на аутсортинге", "price_str": "от 25 000 рублей"},
    {"id": 6, "title": "Оптимизация оплаты труда, численности и затрат на персонал", "price_str": "от 100 000 рублей"},
    {"id": 7, "title": "Коучинг руководителей и управленческих команд", "price_str": "от 30 000 рублей"},
    {"id": 8, "title": "Трудоустройство / карьерный коучинг для ТОП-менеджеров", "price_str": "от 10 000 рублей"},
    # ... все остальные услуги из вашего списка
]

from qdrant_helper import index_services
index_services(services)
```

## 🚀 Использование

### В Telegram боте

Запросы о ценах **автоматически** используют LangGraph RAG:

```python
# Пользователь отправляет:
"Сколько стоит автоматизация HR-процессов?"

# Бот автоматически:
# 1. Определяет, что это запрос о ценах
# 2. Использует LangGraph RAG
# 3. Извлекает точные цены из базы
# 4. Валидирует ответ
# 5. Отправляет пользователю
```

### Через FastAPI

```bash
curl -X POST "http://localhost:8000/rag/query" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Сколько стоит стратегическая сессия?",
    "thread_id": "user_123"
  }'
```

Ответ:
```json
{
  "status": "success",
  "query": "Сколько стоит стратегическая сессия?",
  "answer": "Стратегическая сессия стоит от 85 000 рублей...",
  "sources": ["Стратегическая сессия"],
  "query_type": "pricing",
  "validated": true,
  "method": "langgraph"
}
```

### Программно

```python
from rag_langgraph import query_with_langgraph

result = await query_with_langgraph(
    user_query="Сколько стоит автоматизация HR-процессов?",
    thread_id="user_123"
)

print(result["answer"])
print(f"Валидация: {result['validated']}")
```

## 🧪 Тестирование

```bash
# Запустите тест
python test_langgraph_rag.py
```

Тест проверит:
- ✅ Классификацию запросов (pricing/general)
- ✅ Поиск в Qdrant
- ✅ Извлечение точных цен
- ✅ Валидацию цен
- ✅ Retry logic

## 🔍 Проверка работы

### 1. Проверьте Qdrant

```bash
# Проверьте, что Qdrant запущен
curl http://localhost:6333/collections

# Должно вернуть список коллекций, включая "hr2137_bot_knowledge_base"
```

### 2. Проверьте индексацию

```python
from qdrant_helper import get_qdrant_client

client = get_qdrant_client()
if client:
    collections = client.get_collections()
    print("Коллекции:", collections)
    
    # Проверьте количество точек
    collection_info = client.get_collection("hr2137_bot_knowledge_base")
    print(f"Количество услуг: {collection_info.points_count}")
else:
    print("Qdrant не подключен")
```

### 3. Тестовый запрос

```python
import asyncio
from rag_langgraph import query_with_langgraph

async def test():
    result = await query_with_langgraph(
        "Сколько стоит стратегическая сессия?",
        thread_id="test"
    )
    print(result["answer"])
    print(f"Валидация: {result['validated']}")

asyncio.run(test())
```

## 📋 Checklist для запуска

- [ ] Установлен `langgraph>=0.2.0`
- [ ] Qdrant запущен (локально или Cloud)
- [ ] Установлены `QDRANT_URL` и `QDRANT_API_KEY` (если Cloud)
- [ ] Установлен API ключ для LLM (`OPENROUTER_API_KEY`, `OPENAI_API_KEY`, или `GROQ_API_KEY`)
- [ ] Услуги проиндексированы в Qdrant
- [ ] Тест `test_langgraph_rag.py` проходит успешно

## ❓ Troubleshooting

### Ошибка: Connection refused (Qdrant)

```
❌ Ошибка подключения к Qdrant (http://localhost:6333): [Errno 61] Connection refused
```

**Решение:** Запустите Qdrant:
```bash
docker run -p 6333:6333 qdrant/qdrant
```

Или установите Qdrant Cloud:
```bash
export QDRANT_URL="https://your-cluster.gcp.cloud.qdrant.io"
export QDRANT_API_KEY="your-api-key"
```

### Ошибка: API_KEY not set

```
❌ Ошибка генерации ответа: OPENROUTER_API_KEY not set in environment
```

**Решение:** Установите API ключ:
```bash
export OPENROUTER_API_KEY="your-key"
```

### Ошибка: langgraph not found

```
ModuleNotFoundError: No module named 'langgraph'
```

**Решение:** Установите langgraph:
```bash
pip install langgraph>=0.2.0
```

### Пустые результаты поиска

```
🔍 Найдено 0 услуг для запроса о ценах
```

**Решение:** Проиндексируйте услуги:
```python
from qdrant_helper import index_services
index_services(your_services_list)
```

## 📚 Дополнительная документация

- `RAG_LANGGRAPH.md` - Подробная документация по LangGraph RAG
- `rag_langgraph.py` - Исходный код реализации
- `test_langgraph_rag.py` - Тесты

## 🎯 Следующие шаги

1. Настройте Qdrant и проиндексируйте услуги
2. Запустите тест для проверки
3. Интегрируйте с вашим Telegram ботом (уже интегрировано!)
4. Мониторьте логи для отслеживания валидации цен

## 💡 Советы

- **Для продакшена:** Используйте Qdrant Cloud для надежности
- **Для разработки:** Локальный Qdrant в Docker
- **Мониторинг:** Следите за логами валидации (`validated: True/False`)
- **Обновление цен:** Регулярно переиндексируйте услуги при изменении цен
