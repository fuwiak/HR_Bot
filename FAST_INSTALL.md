# 🚀 Быстрая установка зависимостей

## Проблема
Установка `langchain-core` может зависать при поиске совместимых версий.

## Решение

### Вариант 1: Использовать оптимизированный requirements (рекомендуется)
```bash
pip install --prefer-binary --timeout=100 -r requirements.txt
```

### Вариант 2: Использовать только wheel пакеты (самый быстрый)
```bash
pip install --only-binary=:all: -r requirements_optimized.txt
```

### Вариант 3: Установить конкретные версии напрямую
```bash
pip install langchain-core==0.3.23 langgraph==0.2.40
pip install -r requirements.txt
```

## Что было изменено

1. **requirements.txt**: Зафиксирована версия `langchain-core==0.3.23` для быстрой установки
2. **Dockerfile**: Добавлены флаги `--prefer-binary --timeout=100` для ускорения сборки
3. **requirements_optimized.txt**: Создан оптимизированный файл с зафиксированными версиями

## Docker сборка

Все Dockerfile теперь используют оптимизированную установку:
```dockerfile
RUN pip install --upgrade pip && \
    pip install --prefer-binary --timeout=100 -r requirements.txt
```

Это ускорит сборку Docker образов на Railway и других платформах.
