# Быстрый старт локально (с облачным Qdrant)

## 🚀 Шаг 1: Настройка окружения

```bash
# Создать виртуальное окружение и установить зависимости
./setup_local.sh
```

Этот скрипт:
- ✅ Создаст `.venv` виртуальное окружение
- ✅ Установит все зависимости из `requirements.txt`

---

## 📝 Шаг 2: Настройка .env файла

Создайте файл `.env` на основе примера:

```bash
cp .env.example.local .env
```

Отредактируйте `.env` и укажите минимум:

```bash
# ОБЯЗАТЕЛЬНО:
TELEGRAM_TOKEN=your_telegram_bot_token
OPENROUTER_API_KEY=your_openrouter_api_key

# Ваш облачный Qdrant (из QDRANT_RAILWAY_SETUP.md):
QDRANT_URL=https://239a4026-d673-4b8b-bfab-a99c7044e6b1.us-east4-0.gcp.cloud.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key  # Получить в https://cloud.qdrant.io
```

**Где получить ключи:**
- `TELEGRAM_TOKEN`: @BotFather в Telegram
- `OPENROUTER_API_KEY`: https://openrouter.ai
- `QDRANT_API_KEY`: https://cloud.qdrant.io → ваш кластер → API Keys

---

## 🎯 Шаг 3: Запуск

### Вариант A: Автоматический (рекомендуется)

```bash
./activate_and_run.sh
```

Этот скрипт автоматически:
- Активирует виртуальное окружение
- Запустит бота и веб-интерфейс

### Вариант B: Вручную

```bash
# 1. Активировать виртуальное окружение
source .venv/bin/activate

# 2. Запустить бота
./run_local.sh
```

---

## ✅ Проверка работы

### Telegram бот

1. Найдите бота в Telegram
2. Отправьте `/start`
3. Попробуйте команды:
   - `/rag_stats` - статистика RAG базы
   - `/rag_search подбор персонала` - поиск в базе знаний
   - `/demo_proposal нужна помощь с HR` - генерация КП

### Веб-интерфейс

Откройте в браузере: **http://localhost:8081**

---

## 🔧 Полезные команды

```bash
# Активировать venv
source .venv/bin/activate

# Деактивировать venv
deactivate

# Обновить зависимости
pip install -r requirements.txt --upgrade

# Запустить только бота (без веб-интерфейса)
DISABLE_WEB_INTERFACE=true ./run_local.sh
```

---

## ❗ Troubleshooting

### Ошибка: ".env файл не найден"
```bash
cp .env.example.local .env
# Заполните .env своими ключами
```

### Ошибка: "Qdrant недоступен"
Проверьте:
- Правильный ли URL в `QDRANT_URL`
- Правильный ли `QDRANT_API_KEY`
- Доступен ли Qdrant Cloud кластер

Проверка подключения:
```bash
source .venv/bin/activate
python -c "from qdrant_helper import get_qdrant_client; print(get_qdrant_client())"
```

### Ошибка: "Module not found"
```bash
source .venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: "TELEGRAM_TOKEN не установлен"
Проверьте, что в `.env` файле есть:
```bash
TELEGRAM_TOKEN=your_actual_token_here
```

---

## 📚 Дополнительная документация

- `LOCAL_SETUP.md` - детальная настройка
- `ENV_VARIABLES.md` - все переменные окружения
- `QDRANT_RAILWAY_SETUP.md` - информация о вашем Qdrant кластере










