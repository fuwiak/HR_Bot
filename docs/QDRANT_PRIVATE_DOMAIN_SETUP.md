# 🔧 Настройка Qdrant через приватный домен Railway

## ✅ Решение проблемы таймаутов

Если вы видите ошибки `timed out` при подключении к Qdrant через публичный URL, используйте **приватный домен Railway** `qdrant.railway.internal`.

## 📋 Инструкция по настройке

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Откройте ваш проект
3. Выберите сервис, который использует Qdrant (например, "Telegram Bot" или "Backend API")
4. Перейдите в **Settings → Variables**

### Шаг 2: Добавьте переменные окружения

**Добавьте следующие переменные:**

```env
QDRANT_HOST=qdrant.railway.internal
QDRANT_PORT=6333
```

**Важно:**
- `QDRANT_HOST` должен быть **без** `http://` или `https://`
- Просто: `qdrant.railway.internal`
- `QDRANT_PORT` обычно `6333` (порт по умолчанию)

### Шаг 3: Убедитесь что Qdrant сервис в том же проекте

1. В Railway Dashboard проверьте, что **Qdrant сервис добавлен** в проект
2. Убедитесь что **Qdrant сервис запущен** (статус "Active")
3. Все сервисы должны быть в **одном проекте Railway**

### Шаг 4: Перезапустите сервис

После добавления переменных:
1. Railway Dashboard → ваш сервис → Settings → **Restart**
2. Или просто подождите автоматического перезапуска

## 🔍 Что изменилось в коде

Теперь **`QDRANT_HOST` имеет приоритет** над автоматической переменной `RAILWAY_SERVICE_QDRANT_URL`.

**Приоритет переменных:**
1. ✅ **`QDRANT_HOST`** (явная настройка - приоритет 1)
2. ⚠️ `RAILWAY_SERVICE_QDRANT_URL` (автоматическая переменная Railway - приоритет 2)
3. ⚠️ `qdrant.railway.internal` (fallback, если в Railway но переменные не установлены)
4. ⚠️ `localhost:6333` (локальная разработка)

## ✅ Проверка

После настройки в логах должно быть:

```
🔧 Обнаружен приватный Railway Qdrant сервис: http://qdrant.railway.internal:6333
✅ Используется Qdrant: http://qdrant.railway.internal:6333
🔗 Подключение к Railway Qdrant: http://qdrant.railway.internal:6333 (таймаут: 15.0с)
✅ Qdrant клиент успешно подключен
```

## 🔧 Преимущества приватного домена

- ✅ **Быстрее** - прямое подключение внутри сети Railway
- ✅ **Надежнее** - нет проблем с публичными доменами
- ✅ **Безопаснее** - доступен только внутри проекта Railway
- ✅ **Без таймаутов** - нет проблем с внешними подключениями

## 📋 Чек-лист

- [ ] Переменная `QDRANT_HOST=qdrant.railway.internal` добавлена
- [ ] Переменная `QDRANT_PORT=6333` добавлена
- [ ] Qdrant сервис добавлен в проект Railway
- [ ] Qdrant сервис запущен (статус "Active")
- [ ] Все сервисы в одном проекте Railway
- [ ] Сервис перезапущен после изменения переменных

## ❌ Если не работает

1. **Проверьте имя сервиса Qdrant:**
   - Если ваш Qdrant сервис называется не `qdrant`, используйте: `QDRANT_HOST=<имя-сервиса>.railway.internal`
   - Например: `QDRANT_HOST=qdrant-service.railway.internal`

2. **Проверьте порт:**
   - Убедитесь что порт правильный (обычно `6333`)
   - Проверьте в Railway Dashboard → Qdrant сервис → Settings → Networking

3. **Проверьте что сервисы в одном проекте:**
   - Все сервисы должны быть в одном проекте Railway
   - Приватные домены работают только внутри одного проекта

## 📚 Дополнительная информация

- Подробная диагностика: [QDRANT_TROUBLESHOOTING.md](./QDRANT_TROUBLESHOOTING.md)
- Проверка переменных: [QDRANT_ENV_CHECK_RU.md](./QDRANT_ENV_CHECK_RU.md)
