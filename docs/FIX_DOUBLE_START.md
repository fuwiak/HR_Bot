# 🔧 Исправление ошибок запуска

## Проблемы

1. **`AssertionError: jinja2 must be installed`** - отсутствует зависимость jinja2
2. **`RuntimeError: This Application is already running!`** - приложение пытается запуститься дважды

## ✅ Исправления

### 1. Добавлен jinja2 в requirements.txt:
```txt
jinja2>=3.1.0  # Для Jinja2Templates в FastAPI
```

### 2. Исправлен двойной запуск приложения:

#### Проблема:
- В блоке polling были дублирующиеся вызовы `app.initialize()` и `app.start()`
- При перезапуске контейнера Railway приложение могло пытаться запуститься повторно

#### Решение:
- Добавлена проверка `if not app.running:` перед запуском
- Удалены дублирующиеся вызовы в блоке polling
- Добавлена обработка `RuntimeError` с сообщением "already running"

### 3. Улучшена обработка ошибок:

```python
except RuntimeError as e:
    if "already running" in str(e).lower():
        log.warning("⚠️ Приложение уже запущено, возможно перезапуск контейнера")
        # Ждем завершения существующего процесса
        import time
        time.sleep(5)
    else:
        raise
```

## ✅ Проверка

После исправления:
1. `jinja2` будет установлен при сборке образа
2. Приложение не будет пытаться запуститься дважды
3. При перезапуске контейнера ошибка будет обработана корректно

## 🔄 После исправления

1. Пересоберите образ на Railway
2. Проверьте логи - не должно быть ошибок:
   - `AssertionError: jinja2 must be installed`
   - `RuntimeError: This Application is already running!`
3. Бот должен запуститься корректно

---

**Готово! Ошибки исправлены ✅**
