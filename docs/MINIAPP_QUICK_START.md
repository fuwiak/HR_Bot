# 🚀 Telegram Mini App - Быстрый старт

## ✅ Что сделано

1. **Создан Telegram Mini App в Next.js** (`/frontend/app/miniapp`)
2. **Добавлены все компоненты UI:**
   - Главное меню
   - База знаний (поиск, документы, статистика)
   - Проекты (список, создание, статус, сводка)
   - Инструменты (генерация КП, суммаризация)
   - Помощь (список команд)
   - Чат с AI

3. **Интеграция с Telegram:**
   - Используется `@twa-dev/sdk` для работы с Telegram WebApp API
   - Кнопка "🌐 Открыть Mini App" добавлена в меню `/start`

4. **Настроена конфигурация:**
   - `next.config.js` - headers для Mini App
   - `telegram_bot/config.py` - переменная `MINI_APP_URL`
   - Автоматическое добавление кнопки Web App в меню

## 🔧 Что нужно сделать в Railway

### 1. Установить переменные окружения

**В Frontend сервисе:**
```env
MINI_APP_URL=https://your-frontend-service.up.railway.app/miniapp
# Или используйте:
FRONTEND_URL=https://your-frontend-service.up.railway.app
```

**В Telegram Bot сервисе:**
```env
MINI_APP_URL=https://your-frontend-service.up.railway.app/miniapp
```

### 2. Убедиться, что Frontend имеет HTTPS домен

Railway автоматически предоставляет HTTPS для каждого сервиса. Просто включите **Public Domain** в настройках Frontend сервиса.

### 3. (Опционально) Настроить кнопку меню в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите бота → **Bot Settings** → **Menu Button**
3. URL: `https://your-frontend-service.up.railway.app/miniapp`

## 📦 Установка зависимостей

```bash
cd frontend
npm install
```

Добавлена зависимость: `@twa-dev/sdk`

## 🧪 Тестирование

### Локально:
1. Запустите frontend: `npm run dev`
2. Используйте ngrok: `ngrok http 3000`
3. Установите `MINI_APP_URL=https://your-ngrok-url.ngrok.io/miniapp`
4. Протестируйте в Telegram

### В Railway:
1. Дождитесь деплоя Frontend сервиса
2. Скопируйте HTTPS URL
3. Установите `MINI_APP_URL` в обоих сервисах
4. Перезапустите Telegram Bot сервис
5. Откройте бота и нажмите "🌐 Открыть Mini App"

## 📱 Как пользователи откроют Mini App

1. **Через меню `/start`** - кнопка "🌐 Открыть Mini App" (автоматически)
2. **Через кнопку меню бота** - если настроено в BotFather
3. **Программно** - через `WebApp.openLink()` в коде

## 🎨 Особенности UI

- **Современный дизайн** - градиенты, карточки, анимации
- **Адаптивный** - работает на всех устройствах
- **Haptic Feedback** - тактильная обратная связь
- **Telegram Theme** - автоматическая адаптация под тему Telegram

## 📚 Документация

Полная документация: [RAILWAY_MINIAPP_SETUP.md](./RAILWAY_MINIAPP_SETUP.md)

## ⚠️ Важно

- Mini App **требует HTTPS** (Railway предоставляет автоматически)
- URL должен заканчиваться на `/miniapp`
- Все API запросы идут через `/api/` proxy к Backend

## ✅ Готово!

После настройки Railway, пользователи получат красивое веб-приложение вместо примитивных inline кнопок! 🎉
