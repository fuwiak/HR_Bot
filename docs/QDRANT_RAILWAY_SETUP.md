# 🔍 Qdrant на Railway: Основная векторная база для RAG

## Обзор

Qdrant теперь работает как сервис на Railway и используется как **основная векторная база данных** для RAG (Retrieval-Augmented Generation) системы.

## Архитектура

```
Telegram Bot (Railway)
    │
    ├─► Qdrant (Railway Service) ──► Векторная база знаний
    │   └─► Коллекция: hr2137_bot_knowledge_base
    │
    ├─► OpenRouter API ──► Генерация эмбеддингов
    │   └─► Модель: qwen/qwen3-embedding-8b
    │
    └─► RAG Chain ──► Семантический поиск + LLM
```

## Автоматическое подключение

Бот автоматически подключается к Railway Qdrant сервису если доступны переменные окружения.

### Переменные окружения Railway

Railway автоматически предоставляет эти переменные для Qdrant сервиса:

```bash
# Railway автоматически предоставляет:
QDRANT_HOST="railway-private-domain"  # Внутренний домен Qdrant сервиса
QDRANT_PORT="6333"                    # Порт Qdrant
PORT="6333"                           # Альтернативное имя для порта
RAILWAY_PRIVATE_DOMAIN="..."          # Внутренний домен (если QDRANT_HOST не установлен)

# Опционально (для Qdrant Cloud):
QDRANT_URL="https://..."              # URL Qdrant Cloud (fallback)
QDRANT_API_KEY="..."                  # API ключ Qdrant Cloud (fallback)
```

### Приоритет подключения:

1. **Railway Qdrant сервис** (основной) - если доступен `QDRANT_HOST` или `RAILWAY_PRIVATE_DOMAIN`
2. **Qdrant Cloud** (fallback) - если доступны `QDRANT_URL` и `QDRANT_API_KEY`
3. **Локальный сервер** (разработка) - `http://localhost:6333`

## Настройка на Railway

### 1. Добавить Qdrant сервис

1. В Railway Dashboard → **"+ New"** → **"Database"** → **"Add Qdrant"**
2. Railway автоматически создаст Qdrant сервис
3. Переменные окружения будут автоматически доступны другим сервисам

### 2. Подключить к Telegram Bot сервису

Qdrant автоматически доступен через **Shared Variables**:

- `QDRANT_HOST` - автоматически доступен
- `QDRANT_PORT` - автоматически доступен (6333)
- `RAILWAY_PRIVATE_DOMAIN` - автоматически доступен

**Ничего дополнительно настраивать не нужно!**

### 3. Проверка подключения

После деплоя проверьте логи Telegram Bot сервиса:

```
🔧 Обнаружен Railway Qdrant сервис: http://railway-private-domain:6333
🔗 Подключение к Railway Qdrant: http://railway-private-domain:6333
✅ Qdrant клиент успешно подключен: http://railway-private-domain:6333
✅ Используется Railway Qdrant (основная векторная база для RAG)
```

## Использование

### Программно

```python
from qdrant_helper import get_qdrant_client, search_service, index_services

# Получить клиент Qdrant
client = get_qdrant_client()

# Поиск услуг в векторной базе
results = search_service("Форсайт сессия", limit=5)

# Индексация услуг в Qdrant
services = get_services()  # Из Google Sheets
index_services(services)
```

### RAG Chain

Qdrant автоматически используется в RAG Chain:

```python
from rag_chain import RAGChain

rag = RAGChain()
answer = rag.query("Какие услуги вы предлагаете?")
```

## Коллекция

### Название коллекции

```
hr2137_bot_knowledge_base
```

### Параметры коллекции

- **Размерность векторов:** 1536
- **Метрика расстояния:** Cosine
- **Типы данных:**
  - Услуги (services) - с `source_type: "service"`
  - Документы базы знаний - с `source_type: "document"`

## Индексация данных

### Услуги из Google Sheets

Услуги автоматически индексируются при старте бота:

```python
# Автоматическая индексация в фоне
services = get_services()
index_services(services)
```

### Документы базы знаний

Документы индексируются через веб-интерфейс или API:

```python
from qdrant_loader import QdrantLoader

loader = QdrantLoader()
loader.load_document(file_path, metadata)
```

## Эмбеддинги

### Генерация эмбеддингов

Эмбеддинги генерируются через OpenRouter API:

- **Модель:** `qwen/qwen3-embedding-8b`
- **Размерность:** 1024 (дополняется до 1536 для Qdrant)
- **Поддержка языков:** Русский, английский и другие

### API для эмбеддингов

```python
from qdrant_helper import generate_embedding_async

embedding = await generate_embedding_async("Текст для эмбеддинга")
```

## Мониторинг

### Проверка подключения

```python
from qdrant_helper import get_qdrant_client

client = get_qdrant_client()
if client:
    collections = client.get_collections()
    print(f"✅ Подключено. Коллекций: {len(collections.collections)}")
else:
    print("❌ Не удалось подключиться")
```

### Проверка коллекции

```python
from qdrant_helper import get_qdrant_client

client = get_qdrant_client()
if client:
    collection_info = client.get_collection("hr2137_bot_knowledge_base")
    print(f"Точек в коллекции: {collection_info.points_count}")
    print(f"Размерность: {collection_info.config.params.vectors.size}")
```

### Web UI

Qdrant предоставляет Web UI для просмотра данных:

- **URL:** `https://qdrant-production-bf97.up.railway.app/dashboard`
- **Порт:** 6333

## Troubleshooting

### Ошибка: Connection refused

```
❌ Ошибка подключения к Qdrant: Connection refused
```

**Решение:**
1. Проверьте что Qdrant сервис запущен на Railway
2. Проверьте переменные `QDRANT_HOST` и `QDRANT_PORT`
3. Убедитесь что сервисы в одном проекте Railway

### Ошибка: QDRANT_HOST не установлен

```
⚠️ QDRANT_HOST не найден, используется локальный сервер
```

**Решение:**
1. Убедитесь что Qdrant сервис добавлен в Railway проект
2. Проверьте что Telegram Bot сервис в том же проекте
3. Railway автоматически предоставляет переменные для сервисов

### Данные не индексируются

**Проверьте логи:**
- Если видите `✅ Qdrant клиент успешно подключен` → подключение работает
- Если видите `✅ Успешно проиндексировано N услуг` → индексация работает

### Fallback на Qdrant Cloud

Если Railway Qdrant недоступен, бот автоматически переключится на Qdrant Cloud (если настроен):

```
✅ Используется Qdrant Cloud: https://...cloud.qdrant.io
```

## Производительность

### Railway Qdrant vs Qdrant Cloud vs Локальный

| Критерий | Railway Qdrant | Qdrant Cloud | Локальный |
|----------|----------------|--------------|-----------|
| Скорость | ✅ Быстро (внутренняя сеть) | ✅ Быстро | ✅ Очень быстро |
| Надежность | ✅ Высокая | ✅ Очень высокая | ⚠️ Средняя |
| Масштабируемость | ✅ Высокая | ✅ Очень высокая | ❌ Низкая |
| Стоимость | ✅ Включено в Railway | 💰 Платно | ✅ Бесплатно |

## Рекомендации

1. **Для продакшена:** Используйте Railway Qdrant (автоматически на Railway)
2. **Для разработки:** Можно использовать локальный Qdrant (`docker run -p 6333:6333 qdrant/qdrant`)
3. **Мониторинг:** Следите за логами подключения и индексации
4. **Резервное копирование:** Настройте Qdrant Cloud как fallback

## Дополнительная информация

- `qdrant_helper.py` - Модуль работы с Qdrant
- `rag_chain.py` - RAG Chain с использованием Qdrant
- `qdrant_loader.py` - Загрузка документов в Qdrant
- Railway Dashboard → Qdrant → **Data** - Просмотр данных
- Qdrant Web UI - `https://your-qdrant.up.railway.app/dashboard`