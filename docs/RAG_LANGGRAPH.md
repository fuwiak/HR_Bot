# LangGraph RAG для точного извлечения цен

## Обзор

Реализация RAG с использованием LangGraph для **точного извлечения цен без галлюцинаций**.

## Ключевые возможности

### 1. **Stateful Processing** (Сохранение состояния)
- Состояние запроса сохраняется на всех этапах обработки
- Возможность отслеживания всего процесса
- Поддержка контекста между сессиями

### 2. **Conditional Routing** (Условная маршрутизация)
- Автоматическая классификация запросов: `pricing`, `service_info`, `general`
- Разные стратегии поиска для разных типов запросов
- Специальная обработка запросов о ценах

### 3. **Validation Loop** (Цикл валидации)
- Автоматическая проверка, что цены в ответе соответствуют базе данных
- До 2 повторных попыток при ошибках валидации
- Предупреждения при обнаружении неточностей

### 4. **Точное извлечение цен**
- Извлечение точных цен из payload результатов поиска
- Форматирование специального контекста для LLM
- Запрет на выдумывание цен

## Архитектура

```
Запрос → Классификация → Поиск → Извлечение цен → Форматирование → 
→ Генерация → Валидация → [Retry?] → Финализация → Ответ
```

### Узлы графа:

1. **classify** - Классификация типа запроса
2. **search** - Поиск в RAG (Qdrant)
3. **extract_pricing** - Извлечение точных цен
4. **format** - Форматирование контекста для LLM
5. **generate** - Генерация ответа через LLM
6. **validate** - Валидация цен в ответе
7. **increment_retry** - Увеличение счетчика попыток
8. **finalize** - Финализация ответа

## Установка

```bash
# Добавить langgraph в requirements.txt (уже добавлено)
pip install langgraph>=0.2.0
```

## Использование

### Базовое использование

```python
from rag_langgraph import query_with_langgraph

# Запрос о ценах
result = await query_with_langgraph(
    user_query="Сколько стоит автоматизация HR-процессов?",
    thread_id="user_123"
)

print(result["answer"])
print(f"Валидация: {result['validated']}")
print(f"Услуг найдено: {len(result['sources'])}")
```

### Результат запроса

```python
{
    "answer": "Автоматизация HR-процессов стоит от 80 000 рублей...",
    "sources": ["Автоматизация и настройка HR бизнес-процессов", ...],
    "query_type": "pricing",
    "validated": True,  # Цены прошли валидацию
    "validation_errors": [],
    "retry_count": 0,
    "pricing_info": {
        "services": [...],
        "exact_prices": {...}
    },
    "metadata": {
        "search_results_count": 10,
        "has_pricing_context": True
    }
}
```

### Интеграция в Telegram Bot

В `app.py` автоматически используется LangGraph для запросов о ценах:

```python
# Автоматическое определение запросов о ценах
is_pricing_query = any(kw in text.lower() for kw in [
    "цена", "стоимость", "стоит", "рублей", "руб", "прайс", "сколько",
    "коммерческое предложение", "кп", "расценки", "тарифы"
])

if is_pricing_query:
    # Используется LangGraph RAG
    langgraph_result = await query_with_langgraph(text, thread_id=str(user_id))
```

### Web API

Эндпоинт `/rag/query` автоматически использует LangGraph для запросов о ценах:

```bash
curl -X POST "http://localhost:8000/rag/query" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Сколько стоит стратегическая сессия?",
    "thread_id": "session_123"
  }'
```

Ответ:
```json
{
  "status": "success",
  "query": "Сколько стоит стратегическая сессия?",
  "answer": "Стратегическая сессия стоит от 85 000 рублей...",
  "sources": ["Стратегическая сессия", ...],
  "query_type": "pricing",
  "validated": true,
  "validation_errors": [],
  "method": "langgraph",
  "timestamp": "2026-01-08T..."
}
```

## Преимущества перед стандартным RAG

| Критерий | Стандартный RAG | LangGraph RAG |
|----------|----------------|---------------|
| Точность цен | ❌ Возможны галлюцинации | ✅ Валидация + retry |
| Классификация | ❌ Ручная | ✅ Автоматическая |
| Отслеживание | ❌ Сложно | ✅ Полное состояние |
| Retry logic | ❌ Нет | ✅ До 2 попыток |
| Специализация | ❌ Одна стратегия | ✅ Разные для разных типов |

## Как предотвращаются галлюцинации цен

### 1. Извлечение точных цен из базы

```python
pricing_info["exact_prices"][title.lower()] = exact_price
```

### 2. Специальный контекст для LLM

```
🚨🚨🚨 КРИТИЧЕСКИ ВАЖНО - ТОЧНЫЕ ЦЕНЫ ИЗ БАЗЫ ДАННЫХ 🚨🚨🚨
Используй ТОЛЬКО эти цены, НИКОГДА не выдумывай!

✅ Автоматизация HR-процессов
   💰 ЦЕНА: от 80 000 рублей
   📊 Релевантность: 95.3%

❌ ЗАПРЕЩЕНО выдумывать цены!
✅ ОБЯЗАТЕЛЬНО использовать ТОЧНО указанные цены!
```

### 3. Низкая температура для генерации

```python
temperature=0.3 if query_type == "pricing" else 0.7
```

### 4. Валидация после генерации

```python
# Поиск цен в ответе
price_patterns = [
    r'от\s+(\d+[\s,.]?\d*)\s*(?:руб|₽|рублей|руб\.)',
    r'(\d+[\s,.]?\d*)\s*(?:руб|₽|рублей|руб\.)',
]

# Проверка соответствия с базой
for price_value in found_prices:
    if price_value not in exact_prices:
        errors.append(f"⚠️ Обнаружена цена '{price_value}', которой нет в базе")
```

### 5. Автоматический retry при ошибках

```python
if not validated and retry_count < 2:
    return "retry"  # Возврат к generate_response
```

## Конфигурация

### Параметры валидации

В `rag_langgraph.py`:

```python
state["retry_count"] = 0  # Максимум 2 попытки
```

### Порог релевантности для цен

В `qdrant_helper.py`:

```python
# Для запросов о ценах используется limit=10
results = search_service(query, limit=10)
```

## Логирование

LangGraph RAG предоставляет подробное логирование:

```
🔍 Запрос классифицирован как: PRICING
🔍 Найдено 10 услуг для запроса о ценах
💰 Извлечено 8 услуг с ценами
✅ Ответ сгенерирован (тип: pricing)
✅ Валидация цен прошла успешно
✅ Ответ финализирован (валидация: True)
```

## Пример услуг с ценами

Из вашего списка (из начала чата):

```
✅ Автоматизация и настройка HR бизнес-процессов
   💰 ЦЕНА: от 80 000 рублей

✅ Стратегическая сессия
   💰 ЦЕНА: от 85 000 рублей

✅ Оптимизация и разработка организационной структуры
   💰 ЦЕНА: от 80 000 рублей

✅ HR-сопровождение компании / HRD на аутсортинге
   💰 ЦЕНА: от 25 000 рублей
```

Все эти цены будут **точно** извлечены из базы данных и не будут "выдуманы" LLM.

## Troubleshooting

### Ошибка: langgraph не установлен

```bash
pip install langgraph>=0.2.0
```

### Ошибка: loop уже запущен

LangGraph RAG автоматически обрабатывает эту ситуацию через `concurrent.futures`.

### Валидация не проходит

Проверьте, что в базе данных есть точные цены:
- `price_str` содержит точную цену (например, "от 80 000 рублей")
- Цены индексированы в Qdrant правильно

## Дальнейшее развитие

### Возможные улучшения:

1. **Human-in-the-loop** - Одобрение пользователем перед отправкой
2. **Structured Output** - JSON schema для строгого формата ответа
3. **Multi-agent** - Разные агенты для разных типов запросов
4. **Memory** - Персистентная память между сессиями
5. **Feedback loop** - Улучшение на основе обратной связи

## Сравнение с альтернативами

### vs. LangChain (без Graph)
- ❌ LangChain: Линейная цепочка, сложно добавить retry
- ✅ LangGraph: Условные переходы, циклы, валидация

### vs. Простые функции
- ❌ Функции: Нет сохранения состояния
- ✅ LangGraph: Полное состояние, checkpointing

### vs. Ручная валидация
- ❌ Ручная: Требует кода для каждого случая
- ✅ LangGraph: Декларативное описание графа

## Заключение

LangGraph RAG обеспечивает **точное извлечение цен** из базы данных без галлюцинаций благодаря:

1. ✅ Автоматической классификации запросов
2. ✅ Извлечению точных цен из payload
3. ✅ Специальному контексту для LLM
4. ✅ Валидации с retry logic
5. ✅ Низкой температуре для цен

Это гарантирует, что пользователи получат **только реальные цены из вашей базы данных**.
