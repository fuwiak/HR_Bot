# 🐘 Подключение PostgreSQL к Telegram боту на Railway

## Обзор

Telegram бот теперь поддерживает PostgreSQL для хранения данных пользователей вместо памяти (RAM). Это обеспечивает:

- ✅ **Персистентность данных** - данные сохраняются между перезапусками
- ✅ **Масштабируемость** - можно использовать несколько инстансов бота
- ✅ **Надежность** - данные не теряются при сбоях

## Автоматическое подключение

Бот автоматически подключается к PostgreSQL если доступны переменные окружения Railway.

### Переменные окружения Railway

Railway автоматически предоставляет эти переменные для PostgreSQL сервиса:

```bash
DATABASE_URL="postgresql://postgres:password@railway-private-domain:5432/railway"
DATABASE_PUBLIC_URL="postgresql://postgres:password@railway-tcp-proxy-domain:port/railway"
PGHOST="railway-private-domain"
PGPORT="5432"
PGDATABASE="railway"
PGUSER="postgres"
PGPASSWORD="password"
```

**Бот использует `DATABASE_URL` или `DATABASE_PUBLIC_URL` если они доступны.**

## Что хранится в PostgreSQL

### Таблицы:

1. **user_memory** - История чатов пользователей
   - `user_id` - ID пользователя Telegram
   - `role` - Роль (user/assistant)
   - `text` - Текст сообщения
   - `created_at` - Время создания

2. **user_records** - Записи пользователей
   - `user_id` - ID пользователя
   - `booking_data` - JSON данные записи
   - `created_at` - Время создания

3. **user_data** - Дополнительные данные пользователей
   - `user_id` - ID пользователя (PRIMARY KEY)
   - `phone` - Номер телефона
   - `weeek_workspace` - WEEEK Workspace ID
   - `booking_data` - JSON данные для записи

4. **user_auth** - Данные авторизации
   - `user_id` - ID пользователя (PRIMARY KEY)
   - `auth_data` - JSON данные авторизации

5. **email_subscribers** - Подписчики на email уведомления
   - `user_id` - ID пользователя (PRIMARY KEY)
   - `subscribed_at` - Время подписки

## Fallback на память

Если PostgreSQL недоступен, бот автоматически использует память (RAM):

- ✅ История чатов в `UserMemory`
- ✅ Записи в `UserRecords`
- ✅ Данные пользователей в словарях

**Логи покажут:**
```
⚠️ PostgreSQL модуль не доступен, используем память
```

или

```
✅ PostgreSQL база данных инициализирована
```

## Инициализация базы данных

База данных автоматически инициализируется при старте бота:

1. Проверяется подключение к PostgreSQL
2. Создаются таблицы если их нет
3. Создаются индексы для оптимизации

**Логи:**
```
✅ PostgreSQL connection pool создан через DATABASE_URL
✅ Таблицы базы данных созданы/проверены
✅ PostgreSQL база данных инициализирована
```

## Настройка на Railway

### 1. Добавить PostgreSQL сервис

1. В Railway Dashboard → **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст PostgreSQL сервис
3. Переменные окружения будут автоматически доступны другим сервисам

### 2. Подключить к Telegram Bot сервису

PostgreSQL автоматически доступен через **Shared Variables**:

- `DATABASE_URL` - автоматически доступен
- `PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD` - автоматически доступны

**Ничего дополнительно настраивать не нужно!**

### 3. Проверка подключения

После деплоя проверьте логи:

```
✅ PostgreSQL connection pool создан через DATABASE_URL
✅ Таблицы базы данных созданы/проверены
✅ PostgreSQL база данных инициализирована
```

## Использование

### Программно

Все функции автоматически используют PostgreSQL если доступен:

```python
# История чатов
add_memory(user_id, "user", "Привет!")
history = get_history(user_id)

# Записи пользователей
add_user_record(user_id, {"service": "Консультация", "master": "Анастасия Новосёлова"})
records = get_user_records(user_id)

# Данные пользователей
set_user_phone(user_id, "+79991234567")
phone = get_user_phone(user_id)

# Email подписчики
add_email_subscriber(user_id)
subscribers = get_email_subscribers()
```

### Автоматическое переключение

Бот автоматически переключается между PostgreSQL и памятью:

- ✅ Если PostgreSQL доступен → использует PostgreSQL
- ✅ Если PostgreSQL недоступен → использует память (fallback)

## Миграция данных

Если у вас уже есть данные в памяти:

1. **История чатов** - будет сохранена при следующем сообщении
2. **Записи** - можно экспортировать и импортировать через Google Sheets
3. **Email подписчики** - автоматически мигрируют при следующей подписке

## Мониторинг

### Проверка подключения

```python
from database import get_connection_pool

pool = get_connection_pool()
if pool:
    print("✅ PostgreSQL подключен")
else:
    print("❌ PostgreSQL не подключен")
```

### Проверка таблиц

```sql
-- Подключитесь к PostgreSQL через Railway CLI или Dashboard
SELECT COUNT(*) FROM user_memory;
SELECT COUNT(*) FROM user_records;
SELECT COUNT(*) FROM email_subscribers;
```

## Troubleshooting

### Ошибка: psycopg2 не установлен

```
⚠️ psycopg2 не установлен. Установите: pip install psycopg2-binary
```

**Решение:** Уже добавлено в `requirements.txt`

### Ошибка: Connection refused

```
❌ Ошибка подключения к PostgreSQL
```

**Решение:**
1. Проверьте что PostgreSQL сервис запущен на Railway
2. Проверьте переменные окружения `DATABASE_URL`
3. Убедитесь что сервисы в одном проекте Railway

### Данные не сохраняются

**Проверьте логи:**
- Если видите `⚠️ PostgreSQL модуль не доступен` → PostgreSQL не подключен
- Если видите `✅ PostgreSQL база данных инициализирована` → все работает

### Fallback на память

Если PostgreSQL недоступен, бот продолжит работать используя память:
- ✅ Все функции работают
- ⚠️ Данные теряются при перезапуске
- ⚠️ Не масштабируется на несколько инстансов

## Преимущества PostgreSQL

| Критерий | Память (RAM) | PostgreSQL |
|----------|--------------|------------|
| Персистентность | ❌ Нет | ✅ Да |
| Масштабируемость | ❌ Нет | ✅ Да |
| Надежность | ❌ Низкая | ✅ Высокая |
| Производительность | ✅ Быстро | ✅ Быстро (с индексами) |
| Стоимость | ✅ Бесплатно | ✅ Бесплатно (Railway) |

## Рекомендации

1. **Для продакшена:** Используйте PostgreSQL (автоматически на Railway)
2. **Для разработки:** Можно использовать память локально
3. **Мониторинг:** Следите за логами подключения

## Дополнительная информация

- `database.py` - Модуль работы с PostgreSQL
- `app.py` - Интеграция с ботом
- Railway Dashboard → PostgreSQL → **Data** - Просмотр данных
