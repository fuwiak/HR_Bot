# Деплой HR2137 Bot на Railway

Полная инструкция по развертыванию всех компонентов системы на Railway.

## 📦 Что будет развернуто

1. **Telegram Bot** (app.py) - основной бот на порту 8080 (webhook)
2. **Backend API** (web_interface.py) - FastAPI на порту 8081
3. **Dashboard** (dashboard.py) - RAG Dashboard (опционально, через web_interface.py)

## 🚀 Быстрый старт

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### Шаг 2: Создание проекта на Railway

#### Вариант A: Через веб-интерфейс Railway

1. Перейдите на https://railway.app
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `HR_Bot`
6. Railway автоматически определит Dockerfile и начнет сборку

#### Вариант B: Через Railway CLI

```bash
# Установка CLI
npm i -g @railway/cli

# Вход
railway login

# Создание проекта и линковка
railway init
railway link

# Деплой
railway up
```

### Шаг 3: Настройка переменных окружения

В Railway Dashboard перейдите в **Variables** и добавьте:

#### Обязательные переменные:

```bash
# Telegram Bot
TELEGRAM_TOKEN=your_telegram_bot_token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token  # альтернативное имя

# LLM API
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_MODEL=x-ai/grok-4.1-fast:free

# Порты (Railway автоматически устанавливает PORT)
PORT=8080
WEB_INTERFACE_PORT=8081

# Webhook (Railway автоматически предоставляет домен)
USE_WEBHOOK=true
```

#### Qdrant Vector Database (для RAG):

```bash
QDRANT_URL=https://your-cluster.cloud.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key
```

#### Google Sheets (опционально):

```bash
GOOGLE_SHEETS_SPREADSHEET_ID=1NF25EWqRxjdNTKk4VFVAYZGIOlVFfaktpEvvj1bRXKU
GOOGLE_SHEETS_CREDENTIALS_JSON={"type":"service_account",...}
```

#### Weeek API (для сценариев):

```bash
WEEEEK_TOKEN=e9b78361-0705-408a-af49-ca4300b5cf1b
WEEEK_WORKSPACE_ID=your_workspace_id
WEEEK_API_URL=https://api.weeek.net/public/v1
```

#### HR Time API (для сценариев):

```bash
HRTIME_API_KEY=your_hrtime_api_key
HRTIME_API_URL=https://api.hrtime.ru/v1
HRTIME_PROFILE_ID=29664
```

#### Email (Yandex, для сценариев):

```bash
YANDEX_EMAIL=a-novoselova07@yandex.ru
YANDEX_PASSWORD=your_app_password
YANDEX_IMAP_SERVER=imap.yandex.ru
YANDEX_SMTP_SERVER=smtp.yandex.ru
```

#### Telegram уведомления консультанту:

```bash
TELEGRAM_CONSULTANT_CHAT_ID=your_chat_id
```

#### Мониторинг интервалы (опционально):

```bash
HRTIME_CHECK_INTERVAL_MINUTES=30
EMAIL_CHECK_INTERVAL_MINUTES=15
EMAIL_REQUIRE_APPROVAL=true
DEADLINE_CHECK_INTERVAL_HOURS=24
```

#### Для сессий веб-интерфейса:

```bash
SECRET_KEY=your-random-secret-key-change-in-production
```

### Шаг 4: Настройка домена и webhook

Railway автоматически предоставляет домен для вашего проекта.

1. В Railway Dashboard перейдите в **Settings** → **Networking**
2. Нажмите **"Generate Domain"** (если еще не создан)
3. Скопируйте домен (например: `hr2137-bot-production.up.railway.app`)
4. Railway автоматически установит переменную `RAILWAY_PUBLIC_DOMAIN`

**Важно**: Webhook URL будет: `https://your-domain.up.railway.app/webhook/{TELEGRAM_BOT_TOKEN}`

Бот автоматически настроит webhook при запуске через `start.sh`.

### Шаг 5: Мониторинг деплоя

1. Перейдите в **Deployments** в Railway Dashboard
2. Следите за логами сборки и запуска
3. Проверьте логи на наличие ошибок

### Шаг 6: Проверка работы

После успешного деплоя:

1. **Проверьте Telegram бота**:
   - Отправьте `/start` боту
   - Проверьте что бот отвечает

2. **Проверьте веб-интерфейс**:
   - Откройте `https://your-domain.up.railway.app:8081` (или через Railway Public URL)
   - Должна открыться страница логина (admin/admin)

3. **Проверьте логи**:
   ```bash
   railway logs
   ```
   Или через Dashboard → Deployments → View Logs

## 🔧 Конфигурация для разных сред

### Production

Используйте:
- `USE_WEBHOOK=true` - для масштабируемости
- Надежные секретные ключи
- Qdrant Cloud (не локальный)
- HTTPS (Railway предоставляет автоматически)

### Development/Staging

Можно использовать:
- `USE_WEBHOOK=false` (polling) - для отладки
- Локальный Qdrant (если нужен)
- Тестовые API ключи

## 📊 Мониторинг и логи

### Просмотр логов через Railway CLI:

```bash
railway logs --follow
```

### Просмотр логов через Dashboard:

1. Railway Dashboard → Ваш проект → Deployments
2. Выберите последний деплой
3. Нажмите "View Logs"

### Ключевые сообщения в логах:

- ✅ `✅ Webhook установлен` - бот готов к работе
- ✅ `✅ Бот запущен с webhook` - все работает
- ✅ `✅ Фоновые задачи мониторинга запущены` - сценарии активны
- ❌ Ошибки с API ключами - проверьте переменные окружения

## 🔄 Обновление деплоя

### Автоматический деплой (рекомендуется):

Railway автоматически деплоит при push в подключенную ветку (обычно `main` или `master`).

```bash
git push origin main
```

### Ручной деплой через CLI:

```bash
railway up
```

### Откат к предыдущей версии:

1. Railway Dashboard → Deployments
2. Выберите предыдущую версию
3. Нажмите "Redeploy"

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте `TELEGRAM_TOKEN` в Variables
2. Проверьте логи на ошибки
3. Убедитесь что webhook установлен:
   ```bash
   railway logs | grep webhook
   ```

### Веб-интерфейс недоступен

1. Проверьте что `WEB_INTERFACE_PORT=8081` установлен
2. Проверьте что порт 8081 открыт в Railway Networking
3. Проверьте логи веб-интерфейса

### Ошибки с зависимостями

1. Проверьте что `requirements.txt` актуален
2. Проверьте логи сборки Docker образа
3. Убедитесь что все системные зависимости в Dockerfile

### Проблемы с переменными окружения

1. Убедитесь что все переменные добавлены в Railway Variables
2. Проверьте синтаксис (без пробелов вокруг `=`)
3. Перезапустите сервис после изменения переменных

## 📝 Полный список переменных

См. `ENV_VARIABLES.md` для полного списка всех переменных окружения.

## 🔐 Безопасность

### Рекомендации:

1. **Никогда не коммитьте** `.env` файлы
2. Используйте **Railway Variables** для секретов
3. Используйте **App Passwords** для email
4. Регулярно **ротируйте API ключи**
5. Используйте разные ключи для dev/prod

### Секретные ключи:

- `TELEGRAM_TOKEN` - секретный
- `OPENROUTER_API_KEY` - секретный
- `QDRANT_API_KEY` - секретный
- `SECRET_KEY` - секретный (для сессий)
- Все пароли и токены - секретные

## 📞 Поддержка

При проблемах:
1. Проверьте логи в Railway Dashboard
2. Проверьте документацию: `DEPLOYMENT.md`, `ENV_VARIABLES.md`
3. Проверьте issues в репозитории

## ✅ Чек-лист перед деплоем

- [ ] Все изменения закоммичены и запушены
- [ ] Все обязательные переменные окружения добавлены в Railway
- [ ] TELEGRAM_TOKEN валиден
- [ ] OPENROUTER_API_KEY валиден
- [ ] QDRANT настроен (если используется RAG)
- [ ] Google Sheets credentials добавлены (если используется)
- [ ] Домен сгенерирован в Railway
- [ ] USE_WEBHOOK=true установлен
- [ ] PORT=8080 установлен (Railway автоматически)
- [ ] WEB_INTERFACE_PORT=8081 установлен

## 🎯 После успешного деплоя

1. Протестируйте бота: отправьте `/start`
2. Проверьте веб-интерфейс: откройте URL
3. Проверьте логи на наличие ошибок
4. Настройте мониторинг (если нужно)
5. Добавьте в закладки Railway Dashboard

## 📱 Доступные URL после деплоя

После успешного деплоя будут доступны:

- **Telegram Bot**: Работает через webhook (автоматически настроен)
- **Backend API/Web Interface**: Доступен через Railway Public URL
  - Railway автоматически предоставляет домен типа: `https://your-project.up.railway.app`
  - Веб-интерфейс будет доступен на том же домене (если настроен прокси) или через порт 8081

**Примечание**: Railway обычно открывает один основной порт. В текущей конфигурации:
- Telegram webhook использует порт 8080 (основной PORT)
- Web interface использует порт 8081 (WEB_INTERFACE_PORT)

Если нужно, чтобы оба были доступны через один домен, можно настроить nginx прокси или использовать Railway Networking для нескольких портов.

## 🔄 Команды Railway CLI для управления

```bash
# Просмотр статуса
railway status

# Просмотр логов
railway logs --follow

# Просмотр переменных
railway variables

# Установка переменной
railway variables set KEY=value

# Деплой
railway up

# Открыть проект в браузере
railway open
```

---

**Готово! Ваш бот развернут на Railway 🚀**


