# 🔧 Диагностика проблем с подключением к Qdrant в Railway

## ❌ Симптомы проблемы

Если вы видите в логах:
```
⚠️ Попытка подключения 1/2 не удалась: timed out
HTTP Request: GET https://qdrant-production-bf97.up.railway.app/health "HTTP/1.1 404 Not Found"
❌ Ошибка создания коллекции в Qdrant: timed out
```

## 🔍 Шаг 1: Проверка переменных окружения

### В Railway Dashboard:

1. **Откройте ваш проект Railway**
2. **Перейдите в Variables** (для каждого сервиса, который использует Qdrant)
3. **Проверьте наличие следующих переменных:**

#### Автоматические переменные Railway (должны быть созданы автоматически):
- ✅ `RAILWAY_SERVICE_QDRANT_URL` - автоматически создается Railway для каждого сервиса
  - Формат: `https://qdrant-production-XXXX.up.railway.app` или `http://qdrant.railway.internal:6333`

#### Ручные переменные (если автоматические не работают):
- ✅ `QDRANT_HOST` - публичный или приватный домен Qdrant сервиса
  - Для публичного домена: `https://qdrant-production-bf97.up.railway.app`
  - Для приватного домена: `qdrant.railway.internal`
- ✅ `QDRANT_PORT` - порт Qdrant (по умолчанию 6333, не нужен для публичных доменов)
- ⚠️ `QDRANT_API_KEY` - опционально, только если Qdrant требует аутентификацию

### Приоритет использования переменных (в коде):

1. **Приоритет 1**: `RAILWAY_SERVICE_QDRANT_URL` (автоматическая переменная Railway)
2. **Приоритет 2**: `QDRANT_HOST` (ручная настройка)
3. **Приоритет 3**: `qdrant.railway.internal:6333` (приватный домен, если в Railway)
4. **Приоритет 4**: `localhost:6333` (локальная разработка)

## 🔍 Шаг 2: Проверка сервиса Qdrant в Railway

### Убедитесь что:

1. **Qdrant сервис добавлен в проект Railway**
   - Railway Dashboard → ваш проект → "+ New" → "Database" → "Add Qdrant"
   - Или используйте существующий Qdrant сервис

2. **Qdrant сервис запущен**
   - Railway Dashboard → Qdrant сервис → проверьте статус (должен быть "Active")
   - Проверьте логи Qdrant сервиса на наличие ошибок

3. **Qdrant сервис находится в том же проекте Railway**
   - Все сервисы должны быть в одном проекте для автоматического обнаружения

## 🔍 Шаг 3: Проверка URL Qdrant

### Если используется публичный домен:

1. **Проверьте URL в браузере:**
   ```
   https://qdrant-production-bf97.up.railway.app/health
   ```
   - Должен вернуть `{"status":"ok"}` или `200 OK`
   - Если возвращает `404 Not Found` - URL неправильный или сервис не настроен

2. **Проверьте публичный домен в Railway:**
   - Railway Dashboard → Qdrant сервис → Settings → Networking
   - Убедитесь что публичный домен включен
   - Скопируйте правильный URL

### Если используется приватный домен:

1. **Проверьте что сервисы в одном проекте**
2. **Используйте формат:**
   ```
   QDRANT_HOST=qdrant.railway.internal
   QDRANT_PORT=6333
   ```

## 🔍 Шаг 4: Диагностика таймаутов

### Проблема: "timed out"

**Возможные причины:**

1. **Qdrant сервис не запущен или перезагружается**
   - Решение: Подождите 1-2 минуты и перезапустите ваш сервис

2. **Неправильный URL**
   - Решение: Проверьте URL в Railway Dashboard → Qdrant сервис → Settings → Networking

3. **Сетевые проблемы Railway**
   - Решение: Перезапустите оба сервиса (Qdrant и ваш сервис)

4. **Таймаут слишком короткий**
   - В коде установлен таймаут 15 секунд
   - Если Qdrant медленно отвечает, это может быть проблемой

## ✅ Шаг 5: Правильная настройка переменных

### Вариант 1: Использование автоматической переменной Railway (рекомендуется)

**Ничего не нужно делать!** Railway автоматически создает `RAILWAY_SERVICE_QDRANT_URL`.

**Проверка:**
```bash
# В Railway Dashboard → ваш сервис → Variables
# Должна быть переменная:
RAILWAY_SERVICE_QDRANT_URL=https://qdrant-production-XXXX.up.railway.app
```

### Вариант 2: Ручная настройка через QDRANT_HOST

**Для публичного домена:**
```env
QDRANT_HOST=https://qdrant-production-bf97.up.railway.app
# QDRANT_PORT не нужен для публичных доменов
```

**Для приватного домена:**
```env
QDRANT_HOST=qdrant.railway.internal
QDRANT_PORT=6333
```

### Вариант 3: Использование QDRANT_URL (старый способ, не рекомендуется)

```env
QDRANT_URL=https://qdrant-production-bf97.up.railway.app
QDRANT_API_KEY=your_api_key_if_needed
```

## 🔍 Шаг 6: Проверка в логах

### Что должно быть в логах при успешном подключении:

```
✅ Загружен конфиг qdrant.yaml
✅ Загружен конфиг llm.yaml
🔧 Используется Railway Qdrant из RAILWAY_SERVICE_QDRANT_URL: https://qdrant-production-bf97.up.railway.app
✅ Используется Qdrant: https://qdrant-production-bf97.up.railway.app
🔗 Подключение к публичному Railway Qdrant: https://qdrant-production-bf97.up.railway.app (таймаут: 15.0с)
✅ Qdrant доступен (health check: 200)
✅ Qdrant клиент успешно подключен
```

### Что указывает на проблему:

```
⚠️ Попытка подключения 1/2 не удалась: timed out
⚠️ Не удалось проверить подключение после 2 попыток
❌ Ошибка создания коллекции в Qdrant: timed out
```

## 🛠 Шаг 7: Решение проблем

### Проблема 1: "404 Not Found" на /health endpoint

**Причина:** URL неправильный или Qdrant не настроен на публичный доступ

**Решение:**
1. Проверьте URL в Railway Dashboard → Qdrant сервис → Settings → Networking
2. Убедитесь что публичный домен включен
3. Используйте правильный URL в переменных окружения

### Проблема 2: "timed out" при подключении

**Причина:** Qdrant сервис не отвечает или сетевые проблемы

**Решение:**
1. Проверьте что Qdrant сервис запущен (Railway Dashboard → Qdrant сервис)
2. Перезапустите Qdrant сервис
3. Перезапустите ваш сервис
4. Проверьте логи Qdrant сервиса на наличие ошибок

### Проблема 3: RAILWAY_SERVICE_QDRANT_URL не создается автоматически

**Причина:** Сервисы не в одном проекте или Railway не обнаружил Qdrant

**Решение:**
1. Убедитесь что Qdrant сервис в том же проекте Railway
2. Перезапустите оба сервиса
3. Используйте ручную настройку через `QDRANT_HOST`

### Проблема 4: Неправильный формат URL

**Причина:** URL указан без `https://` или с неправильным форматом

**Решение:**
- Для публичного домена: `https://qdrant-production-bf97.up.railway.app`
- Для приватного домена: `qdrant.railway.internal` (без `http://`)

## 📋 Чек-лист проверки

- [ ] Qdrant сервис добавлен в проект Railway
- [ ] Qdrant сервис запущен и активен
- [ ] Qdrant сервис находится в том же проекте что и ваш сервис
- [ ] Переменная `RAILWAY_SERVICE_QDRANT_URL` существует (или `QDRANT_HOST` установлена)
- [ ] URL правильный (проверен в браузере `/health` endpoint)
- [ ] Публичный домен включен (если используется публичный URL)
- [ ] Сервисы перезапущены после изменения переменных

## 🔗 Полезные ссылки

- [Railway Documentation - Service Variables](https://docs.railway.app/develop/variables)
- [Qdrant Documentation](https://qdrant.tech/documentation/)
- [Railway Qdrant Setup Guide](./QDRANT_RAILWAY_SETUP.md)
