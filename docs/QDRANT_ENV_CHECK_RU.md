# 🔍 Проверка переменных окружения Qdrant

## ❌ Проблема

В логах видны ошибки:
```
⚠️ Попытка подключения 1/2 не удалась: timed out
HTTP Request: GET https://qdrant-production-bf97.up.railway.app/health "HTTP/1.1 404 Not Found"
❌ Ошибка создания коллекции в Qdrant: timed out
```

## ✅ Решение: Проверка переменных окружения

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Откройте ваш проект
3. Выберите сервис, который использует Qdrant (например, "Telegram Bot" или "Backend API")
4. Перейдите в **Settings → Variables**

### Шаг 2: Проверьте наличие переменных

#### ✅ Должна быть одна из этих переменных:

**Вариант 1 (рекомендуется): Автоматическая переменная Railway**
```
RAILWAY_SERVICE_QDRANT_URL=https://qdrant-production-XXXX.up.railway.app
```
- ✅ Эта переменная создается **автоматически** Railway
- ✅ Если её нет, проверьте что Qdrant сервис в том же проекте

**Вариант 2: Ручная настройка через QDRANT_HOST**
```
QDRANT_HOST=https://qdrant-production-bf97.up.railway.app
```
- Для публичного домена: полный URL с `https://`
- Для приватного домена: `qdrant.railway.internal` (без `http://`)

**Вариант 3: Старый способ (не рекомендуется)**
```
QDRANT_URL=https://qdrant-production-bf97.up.railway.app
QDRANT_API_KEY=your_api_key_if_needed
```

### Шаг 3: Проверьте Qdrant сервис

1. В Railway Dashboard убедитесь что **Qdrant сервис добавлен** в проект
2. Убедитесь что **Qdrant сервис запущен** (статус "Active")
3. Убедитесь что **Qdrant сервис в том же проекте** что и ваш сервис

### Шаг 4: Проверьте URL Qdrant

1. Откройте Qdrant сервис в Railway Dashboard
2. Перейдите в **Settings → Networking**
3. Скопируйте **публичный домен** (если используется публичный доступ)
4. Проверьте URL в браузере:
   ```
   https://ваш-qdrant-url.up.railway.app/health
   ```
   - Должен вернуть `{"status":"ok"}` или `200 OK`
   - Если `404 Not Found` - URL неправильный

### Шаг 5: Исправьте переменные (если нужно)

#### Если `RAILWAY_SERVICE_QDRANT_URL` не создается автоматически:

1. **Добавьте переменную вручную:**
   - Railway Dashboard → ваш сервис → Variables → Add Variable
   - Имя: `QDRANT_HOST`
   - Значение: `https://qdrant-production-bf97.up.railway.app` (ваш реальный URL)
   - Или для приватного домена: `qdrant.railway.internal`
   - Если приватный домен, добавьте также: `QDRANT_PORT=6333`

2. **Перезапустите сервис:**
   - Railway Dashboard → ваш сервис → Settings → Restart

### Шаг 6: Запустите скрипт проверки (опционально)

В контейнере можно запустить скрипт для диагностики:

```bash
python scripts/check_qdrant_env.py
```

Этот скрипт покажет:
- Какие переменные установлены
- Какой URL будет использован
- Доступен ли Qdrant

## 📋 Чек-лист

- [ ] Qdrant сервис добавлен в проект Railway
- [ ] Qdrant сервис запущен (статус "Active")
- [ ] Qdrant сервис в том же проекте что и ваш сервис
- [ ] Переменная `RAILWAY_SERVICE_QDRANT_URL` существует ИЛИ `QDRANT_HOST` установлена
- [ ] URL правильный (проверен в браузере `/health` endpoint)
- [ ] Публичный домен включен (если используется публичный URL)
- [ ] Сервисы перезапущены после изменения переменных

## 🔧 Частые проблемы

### Проблема 1: "404 Not Found" на /health

**Причина:** URL неправильный или Qdrant не настроен на публичный доступ

**Решение:**
1. Проверьте URL в Railway Dashboard → Qdrant сервис → Settings → Networking
2. Убедитесь что публичный домен включен
3. Используйте правильный URL в переменных окружения

### Проблема 2: "timed out" при подключении

**Причина:** Qdrant сервис не отвечает или сетевые проблемы

**Решение:**
1. Проверьте что Qdrant сервис запущен
2. Перезапустите Qdrant сервис
3. Перезапустите ваш сервис
4. Проверьте логи Qdrant сервиса

### Проблема 3: RAILWAY_SERVICE_QDRANT_URL не создается

**Причина:** Сервисы не в одном проекте

**Решение:**
1. Убедитесь что Qdrant сервис в том же проекте Railway
2. Перезапустите оба сервиса
3. Используйте ручную настройку через `QDRANT_HOST`

## 📚 Дополнительная информация

- Подробная инструкция: [QDRANT_TROUBLESHOOTING.md](./QDRANT_TROUBLESHOOTING.md)
- Настройка Qdrant в Railway: [QDRANT_RAILWAY_SETUP.md](./QDRANT_RAILWAY_SETUP.md)
