# Настройка Webhook для масштабирования бота

## Обзор изменений

Бот переведен на использование **webhook** вместо polling для лучшей масштабируемости и поддержки **100+ одновременных пользователей**.

### Ключевые улучшения:

1. **Concurrent Updates**: Включена поддержка параллельной обработки обновлений (`concurrent_updates=True`)
2. **Асинхронные HTTP запросы**: `requests` заменен на `aiohttp` для неблокирующих запросов к OpenRouter API
3. **Webhook вместо Polling**: Для production используется webhook (лучше для масштабирования)
4. **Автоматическое определение режима**: Бот автоматически выбирает webhook или polling в зависимости от конфигурации

## Настройка для Railway

### Шаг 1: Получите публичный домен Railway

Railway автоматически предоставляет публичный домен для вашего сервиса. Вы можете найти его:

1. В Railway Dashboard → ваш проект → Settings → Networking
2. Или через переменную окружения `RAILWAY_PUBLIC_DOMAIN` (Railway устанавливает её автоматически)

### Шаг 2: Настройте переменные окружения

Добавьте в Railway следующие переменные:

**Обязательные:**
- `USE_WEBHOOK=true` - включить webhook режим
- `WEBHOOK_URL=https://ваш-домен.railway.app` - публичный URL вашего сервиса
  - Или используйте `RAILWAY_PUBLIC_DOMAIN` (Railway устанавливает автоматически)
- `PORT` - Railway устанавливает автоматически, но можно указать вручную (по умолчанию 8000)

**Пример:**
```
USE_WEBHOOK=true
WEBHOOK_URL=https://your-app-name.up.railway.app
```

**Или используйте автоматический домен Railway:**
```
USE_WEBHOOK=true
RAILWAY_PUBLIC_DOMAIN=your-app-name.up.railway.app
```

### Шаг 3: Убедитесь, что порт открыт

Railway автоматически открывает порт, указанный в переменной `PORT`. Убедитесь, что:
- В Railway Dashboard → Settings → Networking порт открыт для входящих соединений
- Или используйте Railway's автоматическую настройку портов

### Шаг 4: Проверка работы

После деплоя проверьте логи Railway. Должно появиться:

```
🌐 Настройка webhook: https://your-app-name.up.railway.app/webhook/TOKEN
🔌 Порт: 8000
✅ Webhook установлен: https://your-app-name.up.railway.app/webhook/TOKEN
✅ Бот запущен с webhook на порту 8000
📡 Webhook URL: https://your-app-name.up.railway.app/webhook/TOKEN
🚀 Готов к обработке обновлений от Telegram (concurrent_updates=True)
```

## Локальная разработка

Для локальной разработки используйте polling:

```bash
USE_WEBHOOK=false
# или не указывайте USE_WEBHOOK и WEBHOOK_URL
```

Бот автоматически переключится на polling режим.

## Технические детали

### Concurrent Updates

- `concurrent_updates=True` позволяет обрабатывать до 100+ одновременных пользователей
- Каждое обновление обрабатывается в отдельной задаче (task)
- Изоляция сессий per user_id обеспечивается автоматически

### Асинхронные запросы

- Все HTTP запросы к OpenRouter API теперь асинхронные (`aiohttp`)
- Неблокирующая обработка позволяет обрабатывать множество запросов параллельно
- Таймаут: 30 секунд на запрос

### Webhook vs Polling

**Webhook (рекомендуется для production):**
- ✅ Лучше для масштабирования
- ✅ Меньше нагрузка на сервер (Telegram отправляет обновления)
- ✅ Поддержка до 100 одновременных соединений
- ⚠️ Требует публичный URL

**Polling (для локальной разработки):**
- ✅ Не требует публичный URL
- ✅ Проще для отладки
- ❌ Меньше масштабируемость
- ❌ Постоянные запросы к Telegram API

## Troubleshooting

### Бот не получает обновления

1. Проверьте, что `WEBHOOK_URL` указан правильно
2. Убедитесь, что порт открыт в Railway
3. Проверьте логи на наличие ошибок установки webhook
4. Проверьте, что `USE_WEBHOOK=true`

### Ошибка "Webhook URL must be HTTPS"

- Убедитесь, что используете HTTPS URL (не HTTP)
- Railway автоматически предоставляет HTTPS

### Бот работает медленно

- Проверьте, что `concurrent_updates=True` включен (включен по умолчанию)
- Убедитесь, что используете webhook, а не polling
- Проверьте логи на наличие блокирующих операций

## Дополнительные настройки

### Максимальное количество соединений

По умолчанию установлено `max_connections=100`. Это можно изменить в коде:

```python
await app.bot.set_webhook(
    url=full_webhook_url,
    drop_pending_updates=True,
    max_connections=100  # Измените это значение при необходимости
)
```

### Таймауты

Таймаут для HTTP запросов к OpenRouter: 30 секунд (можно изменить в `openrouter_chat` функции).



