# Автоматическая миграция услуг в RAG коллекцию

## Описание

Миграция услуг теперь автоматически выполняется при деплое Backend сервиса на Railway, если установлена переменная окружения `RUN_SERVICES_MIGRATION=true`.

## Как это работает

При старте контейнера Backend сервиса выполняется следующая последовательность:

1. ✅ **Миграции Alembic** - обновление схемы базы данных
2. ✅ **Инициализация Qdrant** - создание коллекции если её нет
3. ✅ **Миграция услуг** (если `RUN_SERVICES_MIGRATION=true`) - загрузка услуг в RAG
4. ✅ **Тесты** (если `RUN_TESTS=true`) - опциональные тесты
5. ✅ **Запуск Backend API** - старт веб-интерфейса

## Настройка на Railway

### Вариант 1: Через Railway Dashboard

1. Откройте Railway Dashboard → проект HR → сервис **HR_Bot** (или Backend)
2. Перейдите в **Variables**
3. Добавьте переменную:
   - **Key**: `RUN_SERVICES_MIGRATION`
   - **Value**: `true`
4. Сохраните и перезапустите сервис

### Вариант 2: Через Railway CLI

```bash
# Выберите сервис
railway service HR_Bot

# Установите переменную
railway variables set RUN_SERVICES_MIGRATION=true
```

## Когда использовать

### ✅ Включить миграцию (`RUN_SERVICES_MIGRATION=true`):

- При первом деплое
- После обновления списка услуг
- Когда нужно перезагрузить услуги в RAG

### ❌ Отключить миграцию (`RUN_SERVICES_MIGRATION=false` или не установлено):

- При обычных деплоях без изменений услуг
- Когда услуги уже загружены и не изменились
- Для ускорения старта контейнера

## Логи

При включенной миграции в логах вы увидите:

```
🔄 Запуск миграций Alembic...
🔧 Инициализация Qdrant коллекции...
📦 Запуск миграции услуг в RAG коллекцию...
✅ Найдено 65 услуг
🚀 Начинаем миграцию 65 услуг в RAG коллекцию...
✅ Успешно загружено 65 услуг в RAG коллекцию 'hr2137_bot_knowledge_base'
🚀 Запуск HR Bot Backend...
```

## Troubleshooting

### Миграция не запускается

1. Проверьте, что переменная `RUN_SERVICES_MIGRATION=true` установлена
2. Проверьте логи контейнера на наличие ошибок
3. Убедитесь, что Qdrant доступен (проверьте `QDRANT_HOST`)

### Ошибка подключения к Qdrant

- Убедитесь, что `QDRANT_HOST` установлен правильно:
  - Для Railway: `qdrant` (private domain) или `https://qdrant-production-bf97.up.railway.app` (public)
  - Проверьте, что Qdrant сервис запущен

### Ошибка генерации эмбеддингов

- Проверьте, что `OPENROUTER_API_KEY` или `OPENAI_API_KEY` установлен
- Проверьте баланс API ключа

## Ручной запуск миграции

Если нужно запустить миграцию вручную без перезапуска контейнера:

```bash
# Через Railway Shell
railway shell --service HR_Bot
python3 scripts/migrate_services_to_rag.py
```

Или локально:

```bash
QDRANT_HOST="https://qdrant-production-bf97.up.railway.app" \
railway run --service HR_Bot python3 scripts/migrate_services_to_rag.py
```
