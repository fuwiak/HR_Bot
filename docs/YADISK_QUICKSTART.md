# 🚀 Yandex Disk Indexer - Быстрый старт

## Что это делает?

**Автоматически индексирует все файлы с Яндекс.Диска в Qdrant Cloud**

Ваша папка: https://disk.yandex.ru/d/-BtoZgh5VMdsPQ

---

## ⚡ Быстрый запуск (3 команды)

```bash
# 1. Проверьте .env (токен уже там!)
cat .env | grep YANDEX_TOKEN

# 2. Установите зависимости (если еще не установлены)
pip install PyPDF2 python-docx pandas openpyxl

# 3. Запустите индексатор
./start_yadisk_indexer.sh
```

**Готово! 🎉**

---

## 📊 Проверка работы

```bash
# Смотрим логи
tail -f logs/yadisk_indexer.out

# Или детальные логи
tail -f yadisk_indexer.log
```

---

## 🛑 Остановка

```bash
./stop_yadisk_indexer.sh
```

---

## ⚙️ Настройки (опционально)

Отредактируйте `.env`:

```env
# Папки для мониторинга (через запятую)
YADISK_WATCH_FOLDERS=/

# Интервал сканирования (секунды)
# 60 = 1 минута, 300 = 5 минут, 3600 = 1 час
YADISK_SCAN_INTERVAL=300

# Макс. размер файла (МБ)
YADISK_MAX_FILE_SIZE=50
```

---

## 📋 Что будет проиндексировано?

✅ Текстовые файлы (`.txt`, `.md`)  
✅ PDF документы (`.pdf`)  
✅ Word документы (`.docx`)  
✅ Excel таблицы (`.xlsx`, `.xls`)  
✅ Данные (`.csv`, `.json`, `.xml`)  

❌ Старый формат Word (`.doc`) - не поддерживается  
❌ Файлы > 50 МБ - пропускаются  

---

## 🔍 Как это работает?

```
Каждые 5 минут:
  1. Сканирует Яндекс.Диск
  2. Находит новые/измененные файлы
  3. Скачивает и извлекает текст
  4. Создает эмбеддинги
  5. Загружает в Qdrant Cloud
```

---

## 💡 Использование в боте

После индексации файлы доступны через:

```bash
# Поиск по базе знаний
/search [запрос]

# RAG ответы
Просто задайте вопрос боту - он найдет ответ в документах!
```

---

## 🐛 Проблемы?

### Индексатор не запускается

```bash
# Проверьте токен
echo $YANDEX_TOKEN

# Проверьте зависимости
pip install -r requirements.txt
```

### Файлы не индексируются

```bash
# Увеличьте лимит размера
echo "YADISK_MAX_FILE_SIZE=100" >> .env
```

### Ошибки в логах

```bash
# Посмотрите детальные ошибки
grep "ERROR" yadisk_indexer.log
grep "❌" logs/yadisk_indexer.out
```

---

## 📚 Полная документация

См. **YADISK_INDEXER_GUIDE.md** для:
- Docker / Railway деплой
- Systemd сервис
- Расширенная настройка
- Мониторинг и отладка

---

## ✅ Готово!

**Индексатор работает в фоне и автоматически обрабатывает все файлы! 🚀**
