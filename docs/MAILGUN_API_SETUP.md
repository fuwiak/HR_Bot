# 🚀 Настройка Mailgun API для отправки с a-novoselova07@yandex.ru

## ✅ Преимущества Mailgun API

- ✅ Можно указать **ЛЮБОЙ** from адрес (включая `a-novoselova07@yandex.ru`)
- ✅ **НЕ требует** подтверждения домена для from адреса
- ✅ Работает через HTTPS (порты SMTP не нужны)
- ✅ Бесплатно: **5000 emails/месяц**

---

## 📋 Шаг 1: Создайте аккаунт Mailgun

1. Перейдите на **https://mailgun.com**
2. Создайте бесплатный аккаунт
3. Подтвердите email

---

## 📋 Шаг 2: Получите API ключ

1. В Mailgun Dashboard перейдите в **Settings** → **API Keys**
2. Найдите **Private API key** (начинается с `key-`)
3. Скопируйте ключ

**Важно:** Используйте **Private API key**, а не Public!

---

## 📋 Шаг 3: Получите домен Mailgun

В Mailgun Dashboard:

1. Перейдите в **Sending** → **Domains**
2. Вы увидите домен типа `sandbox12345.mailgun.org` (для тестирования)
   - Или добавьте свой домен (если есть)

### ⚠️ ВАЖНО: Ограничения Sandbox домена

**Sandbox домен НЕ может отправлять на любые адреса!**

- ❌ Можно отправлять **ТОЛЬКО** на адреса из **Authorized Recipients**
- ❌ Максимум **5 адресов** в бесплатном плане
- ❌ **НЕ подходит** для production

**Для отправки на ЛЮБЫЕ адреса:**
- ✅ Используйте **подтвержденный домен** (например, `bettercallbober.ru`)
- ✅ Или обновите Mailgun план до платного

**Подробнее:** см. `MAILGUN_SANDBOX_LIMITATIONS.md`

---

## 📋 Шаг 4: Добавьте переменные в Railway

В Railway Dashboard → Variables добавьте:

```bash
MAILGUN_API_KEY=key-ваш_api_ключ
MAILGUN_DOMAIN=sandbox12345.mailgun.org  # Или ваш домен
YANDEX_EMAIL=a-novoselova07@yandex.ru
```

**Важно:**
- `MAILGUN_API_KEY` - ваш Private API key из Mailgun
- `MAILGUN_DOMAIN` - домен из Mailgun (sandbox или ваш домен)
- `YANDEX_EMAIL` - адрес, от которого будут отправляться письма

---

## 📋 Шаг 5: Перезапустите сервис

1. В Railway Dashboard нажмите **Deploy** или **Restart**
2. Дождитесь перезапуска

---

## ✅ Проверка работы

Попробуйте отправить письмо через бота. В логах должно быть:

```
📧 Использую Mailgun API: от=a-novoselova07@yandex.ru, к=получатель@example.com
✅ Email отправлен через Mailgun API (ID: ...): получатель@example.com - Тема
```

---

## 🔍 Как это работает

### Приоритет отправки:

1. **Mailgun API** (если `MAILGUN_API_KEY` и `MAILGUN_DOMAIN` установлены)
2. **Resend API** (если `RESEND_API_KEY` установлен)
3. **SMTP** (fallback, может не работать на Railway)

### Формат письма:

- **From:** `a-novoselova07@yandex.ru` (из `YANDEX_EMAIL`)
- **Отправляется через:** Mailgun API
- **Получатель видит:** письмо от `a-novoselova07@yandex.ru` ✅

---

## ❓ Частые вопросы

### Можно ли использовать sandbox домен?

**Да, но с ограничениями!** 

**Sandbox домен:**
- ✅ Работает для тестирования
- ❌ Можно отправлять **ТОЛЬКО** на адреса из **Authorized Recipients**
- ❌ Максимум **5 адресов**
- ❌ **НЕ подходит** для production

**Для production используйте подтвержденный домен!**

### Нужно ли подтверждать домен yandex.ru?

**Нет!** Mailgun API позволяет указать любой from адрес без подтверждения домена.

### Что если превышен лимит?

- Бесплатный план: 5000 emails/месяц
- Если превышен, Mailgun вернет ошибку 402
- Можно обновить план или использовать другой сервис

---

## 🐛 Решение проблем

### Ошибка 401 (Unauthorized):

- Проверьте `MAILGUN_API_KEY` - возможно он неверный
- Убедитесь, что используете **Private API key**, а не Public

### Ошибка 403 (Forbidden):

- Проверьте `MAILGUN_DOMAIN` - возможно домен неверный
- Убедитесь, что домен существует в вашем Mailgun аккаунте

### Ошибка 402 (Payment Required):

- Превышен лимит отправки (5000 emails/месяц)
- Обновите план или подождите до следующего месяца

### Письма не доставляются:

- Если используете sandbox домен, убедитесь, что получатель добавлен в **Authorized Recipients**
  - **Путь:** Send → Sending → Domain Settings → Setup → Authorized Recipients
  - **Важно:** Выберите sandbox домен в правом верхнем углу перед добавлением
- Проверьте логи Railway для детальной ошибки

**Подробная инструкция:** см. `MAILGUN_AUTHORIZED_RECIPIENTS_GUIDE.md`

---

## 📝 Пример полной конфигурации

```bash
# Railway Variables:

# Mailgun API (приоритет 1)
MAILGUN_API_KEY=key-1234567890abcdef
MAILGUN_DOMAIN=sandbox12345.mailgun.org

# Email отправителя
YANDEX_EMAIL=a-novoselova07@yandex.ru

# Остальные переменные
YANDEX_IMAP_PASSWORD=your_password
TELEGRAM_BOT_TOKEN=your_token
# ...
```

---

## ✅ Готово!

Теперь письма будут отправляться через Mailgun API с адреса `a-novoselova07@yandex.ru`!
