# Настройка тестов на Railway

## Обзор

Тесты настроены для автоматического запуска при сборке Docker образа на Railway. Результаты логируются и доступны в Railway logs.

## Конфигурация

### 1. Dockerfile.telegram

Dockerfile обновлен для поддержки условного запуска тестов:

```dockerfile
ENV RUN_TESTS=${RUN_TESTS:-false}
```

- Если `RUN_TESTS=true` - тесты запускаются перед стартом приложения
- Если `RUN_TESTS=false` (по умолчанию) - тесты пропускаются

### 2. Скрипт run_tests.py

Скрипт `run_tests.py` запускает все тесты и логирует результаты:

- ✅ Логирует результаты каждого теста
- ✅ Создает итоговый отчет
- ✅ Сохраняет результаты в `/tmp/test_results.json`
- ✅ Возвращает код выхода (0 = успех, 1 = ошибка)

## Использование на Railway

### Вариант 1: Тесты при каждом деплое (рекомендуется для CI/CD)

В Railway Dashboard → Variables добавьте:

```bash
RUN_TESTS=true
```

Тесты будут запускаться при каждом деплое, результаты будут в логах.

### Вариант 2: Тесты только при необходимости

Оставьте `RUN_TESTS=false` (по умолчанию) и запускайте тесты вручную через Railway CLI:

```bash
railway run python run_tests.py
```

### Вариант 3: Healthcheck с тестами

Можно настроить healthcheck endpoint, который запускает быстрые тесты:

```python
# В app.py или web_interface.py
@app.get("/health")
async def health_check():
    # Быстрая проверка
    from qdrant_helper import get_qdrant_client
    client = get_qdrant_client()
    if not client:
        return {"status": "unhealthy", "reason": "Qdrant not connected"}
    return {"status": "healthy"}
```

## Просмотр результатов тестов

### В Railway Dashboard

1. Откройте ваш сервис на Railway
2. Перейдите в **Deployments** → выберите последний деплой
3. Откройте **Logs**
4. Найдите секцию с тестами:

```
🧪 ТЕСТ: test_forsight_price
✅ ТЕСТ test_forsight_price: ПРОЙДЕН
📊 ИТОГОВЫЙ ОТЧЕТ
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ
```

### Через Railway CLI

```bash
# Просмотр логов
railway logs

# Запуск тестов вручную
railway run python run_tests.py

# Запуск конкретного теста
railway run python test_forsight_price.py
```

## Тесты

### test_forsight_price.py

Проверяет точную цену Форсайт-сессии (90 000 рублей):

- ✅ Тест 1: LangGraph RAG
- ✅ Тест 2: Прямой поиск в Qdrant
- ✅ Тест 3: Проверка базы данных

### test_langgraph_rag.py

Проверяет работу LangGraph RAG:

- ✅ Запросы о ценах
- ✅ Общие запросы
- ✅ Сравнение с стандартным RAG

## Переменные окружения для тестов

Убедитесь, что установлены:

```bash
QDRANT_URL=https://your-cluster.gcp.cloud.qdrant.io
QDRANT_API_KEY=your_api_key
OPENROUTER_API_KEY=your_key
# или
OPENAI_API_KEY=your_key
# или
GROQ_API_KEY=your_key
```

## Автоматизация

### GitHub Actions (опционально)

Можно добавить GitHub Actions для запуска тестов перед деплоем:

```yaml
# .github/workflows/test.yml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
      - run: pip install -r requirements.txt
      - run: python run_tests.py
```

## Troubleshooting

### Тесты не запускаются

1. Проверьте `RUN_TESTS=true` в Railway Variables
2. Проверьте логи сборки Docker образа
3. Убедитесь, что `run_tests.py` скопирован в образ

### Тесты падают

1. Проверьте переменные окружения
2. Проверьте подключение к Qdrant
3. Проверьте логи на ошибки подключения

### Нет результатов в логах

1. Убедитесь, что `PYTHONUNBUFFERED=1` установлен (уже есть в Dockerfile)
2. Проверьте, что тесты не завершаются слишком быстро
3. Добавьте `sleep` в тесты для отладки

## Рекомендации

1. **Для продакшена:** `RUN_TESTS=false` (тесты не замедляют деплой)
2. **Для staging:** `RUN_TESTS=true` (проверка перед продакшеном)
3. **Для разработки:** Запускайте тесты локально перед коммитом

## Мониторинг

Результаты тестов можно мониторить через:

1. **Railway Logs** - в реальном времени
2. **Railway Metrics** - если настроены алерты
3. **Внешний мониторинг** - парсинг логов через API

## Пример логов

```
2026-01-08 19:00:00 - INFO - ======================================================================
2026-01-08 19:00:00 - INFO - 🚀 ЗАПУСК ТЕСТОВ НА RAILWAY
2026-01-08 19:00:00 - INFO - ======================================================================
2026-01-08 19:00:00 - INFO - Время запуска: 2026-01-08T19:00:00
2026-01-08 19:00:00 - INFO - 
2026-01-08 19:00:00 - INFO - ======================================================================
2026-01-08 19:00:00 - INFO - 🧪 ТЕСТ: test_forsight_price
2026-01-08 19:00:00 - INFO - ======================================================================
2026-01-08 19:00:05 - INFO - ✅ ТЕСТ test_forsight_price: ПРОЙДЕН
2026-01-08 19:00:05 - INFO - ======================================================================
2026-01-08 19:00:05 - INFO - 📊 ИТОГОВЫЙ ОТЧЕТ
2026-01-08 19:00:05 - INFO - ======================================================================
2026-01-08 19:00:05 - INFO - Всего тестов: 2
2026-01-08 19:00:05 - INFO - ✅ Прошло: 2
2026-01-08 19:00:05 - INFO - ❌ Провалено: 0
2026-01-08 19:00:05 - INFO - ======================================================================
2026-01-08 19:00:05 - INFO - ✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ
```
