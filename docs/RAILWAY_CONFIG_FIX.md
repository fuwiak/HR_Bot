# 🔧 Исправление: Railway использует .railway/*.toml вместо главных файлов

## ❌ Проблема

Railway при создании нового сервиса автоматически использует `railway.json` или `railway.toml` из корня проекта, которые имеют `dockerfilePath = "Dockerfile"`. Это перезаписывает настройки из `.railway/*.toml`.

## ✅ Решение

### 1. Удален `railway.json`

Файл `railway.json` был удален, так как:
- Railway предпочитает `railway.toml` если оба файла существуют
- Это был дубликат конфигурации
- Удаление заставляет Railway использовать `.railway/*.toml`

### 2. Изменен `railway.toml`

Файл `railway.toml` теперь:
- **НЕ имеет** `dockerfilePath` в секции `[build]`
- Содержит комментарии о том, что нужно использовать `.railway/*.toml`
- Сохраняет другие настройки (deploy, numReplicas, etc.)

### 3. Структура конфигурации

Теперь каждый сервис имеет свой файл в `.railway/`:

```
.railway/
├── telegram-bot.toml  → Dockerfile (главный)
├── backend.toml       → Dockerfile.backend
├── frontend.toml      → frontend/Dockerfile
└── yadisk-indexer.toml → Dockerfile.yadisk
```

## 🎯 Как это работает теперь

1. **При создании нового сервиса:**
   - Railway проверяет `.railway/*.toml` для соответствующего сервиса
   - Если файл существует, использует его конфигурацию
   - Если нет, использует настройки из Railway Dashboard

2. **При использовании Railway CLI:**
   ```bash
   railway link -s MINI-APP
   # Railway автоматически использует .railway/frontend.toml
   ```

3. **При деплое через Git:**
   - Railway автоматически находит соответствующий `.railway/*.toml`
   - Использует `dockerfilePath` из этого файла

## 📋 Проверка

После этих изменений:

1. **Новый сервис MINI-APP:**
   - Railway должен использовать `.railway/frontend.toml`
   - Dockerfile Path должен быть `frontend/Dockerfile`

2. **Существующие сервисы:**
   - Продолжают работать как раньше
   - Используют свои `.railway/*.toml` файлы

## 🔍 Если проблема сохраняется

Если Railway все еще использует `Dockerfile` из корня:

1. **Проверьте в Railway Dashboard:**
   - Settings → Build → Dockerfile Path
   - Должно быть: `frontend/Dockerfile` (для Mini App)

2. **Используйте скрипт:**
   ```bash
   ./scripts/setup_miniapp_dockerfile.sh
   ```

3. **Или установите переменную вручную:**
   ```bash
   railway variables --set "RAILWAY_DOCKERFILE_PATH=frontend/Dockerfile" --service MINI-APP
   ```

## 💡 Почему это работает

- Railway ищет конфигурацию в порядке приоритета:
  1. `.railway/{service-name}.toml` (если сервис связан)
  2. `railway.toml` (если нет dockerfilePath)
  3. `railway.json` (если нет railway.toml)
  4. Настройки из Dashboard

- Удалив `dockerfilePath` из `railway.toml`, Railway будет использовать `.railway/*.toml` для каждого сервиса.
