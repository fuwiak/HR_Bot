# 🔴 Решение проблем с Redis на Railway

## Проблема: Сообщения из Telegram не попадают в Redis

### ✅ Быстрая проверка

1. **Проверьте, что Redis сервис добавлен в проект Railway:**
   - Railway Dashboard → Ваш проект
   - Должен быть сервис "Redis" (тип: Database)
   - Если нет → "+ New" → "Database" → "Add Redis"

2. **Проверьте логи Telegram Bot сервиса:**
   
   Ищите эти сообщения при запуске:
   ```
   ✅ Redis клиент создан через REDIS_URL
   ✅ Redis подключение успешно через REDIS_URL
   🔄 Запущена фоновая синхронизация Redis -> PostgreSQL (каждые 5 минут)
   ```
   
   Если видите:
   ```
   ⚠️ Redis переменные окружения не установлены или подключение не удалось
   ```
   → Redis сервис не подключен или переменные не передаются

3. **Проверьте переменные окружения:**
   
   Railway Dashboard → Telegram Bot сервис → Variables
   
   Должны быть доступны (автоматически от Redis сервиса):
   - `REDIS_URL` - должен быть установлен
   - `REDISHOST` - должен быть установлен
   - `REDISPORT` - должен быть установлен
   - `REDISPASSWORD` - должен быть установлен
   - `REDISUSER` - должен быть установлен (обычно "default")

### 🔧 Решение проблем

#### Проблема 1: Redis сервис не добавлен

**Решение:**
1. Railway Dashboard → Ваш проект
2. "+ New" → "Database" → "Add Redis"
3. Дождитесь создания сервиса
4. Перезапустите Telegram Bot сервис (Redeploy)

#### Проблема 2: Переменные Redis не передаются в Telegram Bot

**Решение:**
1. Убедитесь, что Redis и Telegram Bot сервисы в **одном проекте Railway**
2. Railway Dashboard → Telegram Bot сервис → Settings → Variables
3. Проверьте, что видны переменные от Redis (они должны быть автоматически)
4. Если нет → Railway Dashboard → Redis сервис → Settings → Variables → скопируйте значения
5. Railway Dashboard → Telegram Bot сервис → Variables → Add Variable → вставьте вручную

#### Проблема 3: Ошибка подключения к Redis

**В логах видите:**
```
❌ Ошибка подключения через REDIS_URL: Connection refused
```

**Решение:**
1. Проверьте, что Redis сервис запущен (Railway Dashboard → Redis → должен быть статус "Running")
2. Проверьте, что Redis и Telegram Bot в одном проекте
3. Перезапустите оба сервиса

#### Проблема 4: Сообщения сохраняются только в PostgreSQL

**Это нормально!** Бот работает с fallback механизмом:
- ✅ Если Redis недоступен → использует PostgreSQL (медленнее, но работает)
- ✅ Если Redis + PostgreSQL недоступны → использует RAM

**Для ускорения работы:**
1. Добавьте Redis сервис (см. Проблема 1)
2. Убедитесь, что переменные передаются (см. Проблема 2)

### 📊 Проверка работы Redis

После исправления проблем проверьте логи:

**При запуске бота:**
```
🔍 Проверка Redis переменных: REDIS_URL=установлен, REDIS_HOST=установлен, ...
✅ Redis клиент создан через REDIS_URL
✅ Redis подключение успешно через REDIS_URL
🔄 Запущена фоновая синхронизация Redis -> PostgreSQL (каждые 5 минут)
```

**При получении сообщения:**
```
✅ Сообщение сохранено в Redis (user_id=123, role=user)
✅ Сообщение сохранено в PostgreSQL (id=456, user_id=123)
```

**Каждые 5 минут (фоновая синхронизация):**
```
🔄 Начало фоновой синхронизации Redis -> PostgreSQL...
✅ Фоновая синхронизация завершена: X пользователей
```

### 🎯 Итоговая проверка

1. ✅ Redis сервис добавлен в проект
2. ✅ Redis сервис запущен (статус "Running")
3. ✅ Telegram Bot сервис видит переменные Redis
4. ✅ В логах видно "✅ Redis подключение успешно"
5. ✅ В логах видно "🔄 Запущена фоновая синхронизация"

Если все пункты выполнены, но сообщения все равно не попадают в Redis:
- Проверьте логи на наличие ошибок при сохранении
- Убедитесь, что используется последняя версия кода
- Проверьте, что `save_to_redis=True` в вызовах `save_telegram_message()`

### 📝 Дополнительная информация

- Подробная документация: `docs/REDIS_SETUP.md`
- Код подключения: `services/helpers/redis_helper.py`
- Сохранение сообщений: `backend/database/message_storage.py`
