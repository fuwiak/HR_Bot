# 🔧 Как работает SMTP Relay в коде

## ⚠️ ВАЖНО: Проблема с Mailgun и yandex.ru

**Mailgun требует подтверждения домена для отправки с него!**

Если вы хотите отправлять с `a-novoselova07@yandex.ru`:
- ❌ Mailgun SMTP relay **НЕ подойдет** - вы не можете подтвердить домен `yandex.ru` (это не ваш домен)
- ❌ Mailgun отклонит письма или пометит их как спам

**Решения:**
1. ✅ Использовать **Mailgun API** (не SMTP) - позволяет указать любой from адрес
2. ✅ Использовать другой сервис, который не требует подтверждения домена
3. ✅ Использовать **Render.com** или другой хостинг, где SMTP порты открыты

---

## 📋 Что происходит, когда вы устанавливаете переменные:

```bash
SMTP_RELAY_SERVER=smtp.mailgun.org
SMTP_RELAY_PORT=587
SMTP_RELAY_USER=postmaster@your-domain.mailgun.org
SMTP_RELAY_PASSWORD=your_mailgun_password
YANDEX_EMAIL=a-novoselova07@yandex.ru
```

---

## 🔄 Логика работы кода:

### Шаг 1: Проверка переменных

Код проверяет, установлен ли `SMTP_RELAY_SERVER`:

```python
SMTP_RELAY_SERVER = os.getenv("SMTP_RELAY_SERVER")  # smtp.mailgun.org
SMTP_RELAY_PORT = os.getenv("SMTP_RELAY_PORT")      # 587
SMTP_RELAY_USER = os.getenv("SMTP_RELAY_USER")       # postmaster@your-domain.mailgun.org
SMTP_RELAY_PASSWORD = os.getenv("SMTP_RELAY_PASSWORD") # your_mailgun_password
```

### Шаг 2: Выбор SMTP сервера

**Если `SMTP_RELAY_SERVER` установлен:**
- ✅ Используется `smtp.mailgun.org` вместо `smtp.yandex.ru`
- ✅ Используется порт `587` (или указанный в `SMTP_RELAY_PORT`)
- ✅ Для авторизации используется `SMTP_RELAY_USER` и `SMTP_RELAY_PASSWORD`

**Если `SMTP_RELAY_SERVER` НЕ установлен:**
- Используется `smtp.yandex.ru` (стандартный Yandex SMTP)
- Используется `YANDEX_EMAIL` и `YANDEX_PASSWORD` для авторизации

### Шаг 3: Отправка письма

**Важно:** Поле "From" в письме **всегда** остается `YANDEX_EMAIL` (a-novoselova07@yandex.ru)

```python
message["From"] = YANDEX_EMAIL  # a-novoselova07@yandex.ru
```

Это означает:
- 📧 Письмо отправляется **через** Mailgun SMTP сервер
- 📧 Но **от адреса** a-novoselova07@yandex.ru
- 📧 Получатель увидит письмо от a-novoselova07@yandex.ru

---

## 📊 Схема работы:

### БЕЗ SMTP Relay (стандартный Yandex):
```
Бот → smtp.yandex.ru:465 → Получатель
     (авторизация: a-novoselova07@yandex.ru)
     (From: a-novoselova07@yandex.ru)
```

### С SMTP Relay (Mailgun):
```
Бот → smtp.mailgun.org:587 → Mailgun → Получатель
     (авторизация: postmaster@your-domain.mailgun.org)
     (From: a-novoselova07@yandex.ru) ← ВАЖНО!
```

---

## 🔍 Что видно в логах:

Когда используется SMTP Relay, в логах будет:

```
📧 Использую SMTP Relay: smtp.mailgun.org:587
📧 Отправка email: от=a-novoselova07@yandex.ru, к=получатель@example.com, тема=Тема, server=smtp.mailgun.org, port=587
🔄 Попытка 2: Порт 587 (STARTTLS)...
✅ Email отправлен (sync, порт 587): получатель@example.com - Тема
```

---

## ⚠️ Важные моменты:

### 1. Авторизация на Mailgun
- Используется `SMTP_RELAY_USER` и `SMTP_RELAY_PASSWORD`
- Это ваши Mailgun credentials, **НЕ** Yandex пароль

### 2. Поле "From" в письме
- Всегда остается `YANDEX_EMAIL` (a-novoselova07@yandex.ru)
- Получатель увидит письмо от a-novoselova07@yandex.ru
- Mailgun просто пересылает письмо, не меняя адрес отправителя

### 3. Порт
- Mailgun обычно использует порт **587** (STARTTLS)
- Или порт **465** (SSL)
- Укажите правильный порт в `SMTP_RELAY_PORT`

---

## ✅ Проверка работы:

После установки переменных:

1. **Перезапустите сервис в Railway**
2. **Попробуйте отправить письмо через бота**
3. **Проверьте логи:**
   - Должно быть: `📧 Использую SMTP Relay: smtp.mailgun.org:587`
   - Должно быть: `✅ Email отправлен`

4. **Проверьте письмо:**
   - От кого: `a-novoselova07@yandex.ru` ✅
   - Получено через: Mailgun сервер ✅

---

## 🐛 Если не работает:

### Ошибка авторизации:
- Проверьте `SMTP_RELAY_USER` и `SMTP_RELAY_PASSWORD`
- Убедитесь, что это правильные Mailgun credentials

### Ошибка подключения:
- Проверьте `SMTP_RELAY_SERVER` (должно быть `smtp.mailgun.org`)
- Проверьте `SMTP_RELAY_PORT` (обычно `587`)

### Письмо не отправляется:
- Проверьте логи Railway
- Убедитесь, что все переменные установлены правильно
- Проверьте, что Mailgun аккаунт активен

---

## 📝 Пример полной конфигурации:

```bash
# Railway Variables:

# Email отправителя (будет в поле "From")
YANDEX_EMAIL=a-novoselova07@yandex.ru

# SMTP Relay настройки (Mailgun)
SMTP_RELAY_SERVER=smtp.mailgun.org
SMTP_RELAY_PORT=587
SMTP_RELAY_USER=postmaster@mg.yourdomain.com
SMTP_RELAY_PASSWORD=your_mailgun_smtp_password

# Остальные переменные (не меняются)
YANDEX_IMAP_PASSWORD=your_yandex_password
TELEGRAM_BOT_TOKEN=your_token
# ...
```

---

## 🎯 Итого:

**Код автоматически:**
1. ✅ Определяет, что установлен `SMTP_RELAY_SERVER`
2. ✅ Использует Mailgun SMTP сервер вместо Yandex
3. ✅ Авторизуется через Mailgun credentials
4. ✅ Отправляет письмо с адресом `a-novoselova07@yandex.ru`

**Никаких изменений в коде не требуется** - просто установите переменные и перезапустите сервис!

